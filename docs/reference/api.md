# API reference

Status: generated from the complete top-level `agnoclaw.__all__` contract

> Do not edit this file by hand. Run
> `uv run python scripts/generate_api_reference.py` and review the diff.

Reference schema: `1.0`
Public symbols: `398`
Public-surface digest: `sha256:af3d959ddbdc38d0dceb52e0045d46461d2326282052e5a57efafe204b60c311`

Every name below is importable directly from `agnoclaw`. Signatures are
generated from the installed runtime objects; source modules identify the
implementation owner, not an additional supported import path.

## `agnoclaw`

### `CONTEXT_SCHEMA_VERSION`

Constant · `agnoclaw.CONTEXT_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import CONTEXT_SCHEMA_VERSION

CONTEXT_SCHEMA_VERSION = '1.0'
```

### `DEAD_LETTER_AUDIT_SCOPE`

Constant · `agnoclaw.DEAD_LETTER_AUDIT_SCOPE`

Versioned public value.

```python
from agnoclaw import DEAD_LETTER_AUDIT_SCOPE

DEAD_LETTER_AUDIT_SCOPE = 'runtime.dead_letters.audit'
```

### `DEAD_LETTER_INSPECT_SCOPE`

Constant · `agnoclaw.DEAD_LETTER_INSPECT_SCOPE`

Versioned public value.

```python
from agnoclaw import DEAD_LETTER_INSPECT_SCOPE

DEAD_LETTER_INSPECT_SCOPE = 'runtime.dead_letters.inspect'
```

### `DEAD_LETTER_REQUEUE_SCOPE`

Constant · `agnoclaw.DEAD_LETTER_REQUEUE_SCOPE`

Versioned public value.

```python
from agnoclaw import DEAD_LETTER_REQUEUE_SCOPE

DEAD_LETTER_REQUEUE_SCOPE = 'runtime.dead_letters.requeue'
```

### `DOCKER_EVALUATION_POLICY_VERSION`

Constant · `agnoclaw.DOCKER_EVALUATION_POLICY_VERSION`

Versioned public value.

```python
from agnoclaw import DOCKER_EVALUATION_POLICY_VERSION

DOCKER_EVALUATION_POLICY_VERSION = 'agnoclaw.improvement.docker.v1'
```

### `EVALUATION_CORPUS_SCHEMA_VERSION`

Constant · `agnoclaw.EVALUATION_CORPUS_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import EVALUATION_CORPUS_SCHEMA_VERSION

EVALUATION_CORPUS_SCHEMA_VERSION = '1.0'
```

### `EVALUATION_RUNNER_SCHEMA_VERSION`

Constant · `agnoclaw.EVALUATION_RUNNER_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import EVALUATION_RUNNER_SCHEMA_VERSION

EVALUATION_RUNNER_SCHEMA_VERSION = '1.2'
```

### `IMPROVEMENT_RUNNER_IMPLEMENTATION_DIGEST`

Constant · `agnoclaw.IMPROVEMENT_RUNNER_IMPLEMENTATION_DIGEST`

Versioned public value.

```python
from agnoclaw import IMPROVEMENT_RUNNER_IMPLEMENTATION_DIGEST

IMPROVEMENT_RUNNER_IMPLEMENTATION_DIGEST = 'sha256:cc7c407f69635b8e8e001b4d2f4c89b0bd250c6c73ebefbca307aac89f9f129e'
```

### `LIFECYCLE_PROTOCOL_VERSION`

Constant · `agnoclaw.LIFECYCLE_PROTOCOL_VERSION`

Versioned public value.

```python
from agnoclaw import LIFECYCLE_PROTOCOL_VERSION

LIFECYCLE_PROTOCOL_VERSION = '1.0'
```

### `MIGRATION_012_MANIFEST_SCHEMA_VERSION`

Constant · `agnoclaw.MIGRATION_012_MANIFEST_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import MIGRATION_012_MANIFEST_SCHEMA_VERSION

MIGRATION_012_MANIFEST_SCHEMA_VERSION = '1.0'
```

### `MIGRATION_012_PLAN_SCHEMA_VERSION`

Constant · `agnoclaw.MIGRATION_012_PLAN_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import MIGRATION_012_PLAN_SCHEMA_VERSION

MIGRATION_012_PLAN_SCHEMA_VERSION = '1.0'
```

### `MIGRATION_012_ROLLBACK_BOUNDARY`

Constant · `agnoclaw.MIGRATION_012_ROLLBACK_BOUNDARY`

Versioned public value.

```python
from agnoclaw import MIGRATION_012_ROLLBACK_BOUNDARY

MIGRATION_012_ROLLBACK_BOUNDARY = 'before-explicit-schema-contraction-v1'
```

### `MIGRATION_PREFLIGHT_SCHEMA_VERSION`

Constant · `agnoclaw.MIGRATION_PREFLIGHT_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import MIGRATION_PREFLIGHT_SCHEMA_VERSION

MIGRATION_PREFLIGHT_SCHEMA_VERSION = '1.0'
```

### `OPERATION_RECONCILIATION_EVIDENCE_PURPOSE`

Constant · `agnoclaw.OPERATION_RECONCILIATION_EVIDENCE_PURPOSE`

Versioned public value.

```python
from agnoclaw import OPERATION_RECONCILIATION_EVIDENCE_PURPOSE

OPERATION_RECONCILIATION_EVIDENCE_PURPOSE = 'operation.reconciliation.evidence'
```

### `POSTGRES_MIGRATION_012_LIFECYCLE_SCHEMA_VERSION`

Constant · `agnoclaw.POSTGRES_MIGRATION_012_LIFECYCLE_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import POSTGRES_MIGRATION_012_LIFECYCLE_SCHEMA_VERSION

POSTGRES_MIGRATION_012_LIFECYCLE_SCHEMA_VERSION = '1.0'
```

### `POSTGRES_MIGRATION_012_PLAN_SCHEMA_VERSION`

Constant · `agnoclaw.POSTGRES_MIGRATION_012_PLAN_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import POSTGRES_MIGRATION_012_PLAN_SCHEMA_VERSION

POSTGRES_MIGRATION_012_PLAN_SCHEMA_VERSION = '1.0'
```

### `POSTGRES_MIGRATION_012_ROLLBACK_BOUNDARY`

Constant · `agnoclaw.POSTGRES_MIGRATION_012_ROLLBACK_BOUNDARY`

Versioned public value.

```python
from agnoclaw import POSTGRES_MIGRATION_012_ROLLBACK_BOUNDARY

POSTGRES_MIGRATION_012_ROLLBACK_BOUNDARY = 'before-first-legitimate-post-cutover-target-write-v1'
```

### `POSTGRES_MIGRATION_012_SCAN_SCHEMA_VERSION`

Constant · `agnoclaw.POSTGRES_MIGRATION_012_SCAN_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import POSTGRES_MIGRATION_012_SCAN_SCHEMA_VERSION

POSTGRES_MIGRATION_012_SCAN_SCHEMA_VERSION = '1.0'
```

### `POSTGRES_MIGRATION_012_SCHEDULE_MAP_SCHEMA_VERSION`

Constant · `agnoclaw.POSTGRES_MIGRATION_012_SCHEDULE_MAP_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import POSTGRES_MIGRATION_012_SCHEDULE_MAP_SCHEMA_VERSION

POSTGRES_MIGRATION_012_SCHEDULE_MAP_SCHEMA_VERSION = '1.1'
```

### `POSTGRES_MIGRATION_012_TRANSFORM_SCHEMA_VERSION`

Constant · `agnoclaw.POSTGRES_MIGRATION_012_TRANSFORM_SCHEMA_VERSION`

Versioned public value.

```python
from agnoclaw import POSTGRES_MIGRATION_012_TRANSFORM_SCHEMA_VERSION

POSTGRES_MIGRATION_012_TRANSFORM_SCHEMA_VERSION = '1.0'
```

### `PROCESS_EVALUATION_PROTOCOL_VERSION`

Constant · `agnoclaw.PROCESS_EVALUATION_PROTOCOL_VERSION`

Versioned public value.

```python
from agnoclaw import PROCESS_EVALUATION_PROTOCOL_VERSION

PROCESS_EVALUATION_PROTOCOL_VERSION = 'agnoclaw.improvement.process.v1'
```

### `RECONCILIATION_EVIDENCE_PURPOSE`

Constant · `agnoclaw.RECONCILIATION_EVIDENCE_PURPOSE`

Versioned public value.

```python
from agnoclaw import RECONCILIATION_EVIDENCE_PURPOSE

RECONCILIATION_EVIDENCE_PURPOSE = 'learning.reconciliation.evidence'
```

### `RUN_INSPECT_SCOPE`

Constant · `agnoclaw.RUN_INSPECT_SCOPE`

Versioned public value.

```python
from agnoclaw import RUN_INSPECT_SCOPE

RUN_INSPECT_SCOPE = 'runtime:run:inspect'
```

## `agnoclaw.agent`

### `AgentHarness`

Class · `agnoclaw.agent.AgentHarness`

Embeddable Agno runtime for short, long, and controllable agent work.

```python
from agnoclaw import AgentHarness

AgentHarness(
    model: 'str | Model | AgnoModelFactory | None' = None,
    *,
    provider: 'str | None' = None,
    profile: 'str | RuntimeProfile | None' = None,
    cache_prompts: 'bool | None' = None,
    effort: 'str | None' = None,
    session_id: 'str | None' = None,
    user_id: 'str | None' = None,
    workspace_dir: 'str | Path | None' = None,
    allow_workspace_hooks: 'bool' = False,
    workspace_hook_env_allowlist: 'list[str] | tuple[str, ...] | None' = None,
    sandbox_dir: 'str | Path | None' = None,
    sandbox_mode: 'str | SandboxMode | None' = None,
    include_default_tools: 'bool' = True,
    tools: 'list | None' = None,
    capabilities: 'list | tuple | None' = None,
    capability_invoker: 'CapabilityInvoker | None' = None,
    instructions: 'str | None' = None,
    config: 'HarnessConfig | None' = None,
    db=None,
    name: 'str' = 'agnoclaw',
    agent_id: 'str | None' = None,
    debug: 'bool' = False,
    subagents: 'dict | None' = None,
    enable_user_memory: 'bool' = False,
    enable_learning: 'bool | None' = None,
    learning_mode: 'str | None' = None,
    learning_namespace: 'str | None' = None,
    learning_knowledge: 'Any | None' = None,
    enable_session_context: 'bool | None' = None,
    learning: 'LearningPolicy | None' = None,
    enable_compression: 'bool | None' = None,
    compress_token_limit: 'int | None' = None,
    enable_session_summary: 'bool | None' = None,
    num_history_runs: 'int | None' = None,
    num_history_messages: 'int | None' = None,
    max_tool_calls_from_history: 'int | None' = None,
    max_context_tokens: 'int | None' = None,
    auto_compact_context: 'bool | None' = None,
    context_lock_provider: 'ContextLockProvider | None' = None,
    max_inline_output_chars: 'int | None' = None,
    output_schema: 'type | dict[str, Any] | None' = None,
    parser_model: 'Any | None' = None,
    parser_model_prompt: 'str | None' = None,
    output_model: 'Any | None' = None,
    output_model_prompt: 'str | None' = None,
    parse_response: 'bool' = True,
    structured_outputs: 'bool | None' = None,
    use_json_mode: 'bool' = False,
    event_sink: 'EventSink | None' = None,
    event_sink_mode: 'str | None' = None,
    policy_engine: 'PolicyEngine | None' = None,
    policy_fail_open: 'bool | None' = None,
    permission_mode: 'str | None' = None,
    permission_approver: 'PermissionApprover | None' = None,
    permission_require_approver: 'bool | None' = None,
    permission_preapproved_tools: 'list[str] | tuple[str, ...] | None' = None,
    permission_preapproved_categories: 'list[str] | tuple[str, ...] | None' = None,
    backend: 'RuntimeBackend | None' = None,
    runtime_store: 'RuntimeStore | None' = None,
    artifact_store: 'ArtifactStore | None' = None,
    learning_ledger: 'LearningLedger | None' = None,
    learning_promotion_adapter: 'LearningPromotionAdapter | None' = None,
    skill_install_approver: 'SkillInstallApprover | None' = None,
    context_providers: 'list[ContextProvider] | tuple[ContextProvider, ...] | None' = None,
    dependencies: 'dict[str, Any] | None' = None,
    add_dependencies_to_context: 'bool' = False,
    session_state: 'dict[str, Any] | None' = None,
    add_session_state_to_context: 'bool' = False,
    packs: 'list[str | Path] | tuple[str | Path, ...] | None' = None,
    trusted_packs: 'bool' = False,
    pre_run_hooks: 'list[PreRunHook] | None' = None,
    post_run_hooks: 'list[PostRunHook] | None' = None,
    tenant_id: 'str | None' = None,
    org_id: 'str | None' = None,
    team_id: 'str | None' = None,
    roles: 'list[str] | tuple[str, ...] | None' = None,
    scopes: 'list[str] | tuple[str, ...] | None' = None,
    request_id: 'str | None' = None,
    trace_id: 'str | None' = None,
    context_metadata: 'dict[str, Any] | None' = None,
    skills_dirs: 'list[tuple[str | Path, str]] | None' = None,
    on_compaction: 'Callable[[str], Awaitable[None]] | None' = None,
    on_session_end: 'Callable[..., Awaitable[None] | None] | None' = None,
    session_metadata: 'dict[str, Any] | None' = None,
    model_id: 'str | None' = None,
    extra_tools: 'list | None' = None,
    extra_instructions: 'str | None' = None,
)
```

### `get_current_dependencies`

Function · `agnoclaw.agent.get_current_dependencies`

Return ``dependencies`` from the active run context, if any.

```python
from agnoclaw import get_current_dependencies

get_current_dependencies() -> 'dict[str, Any] | None'
```

### `get_current_run_context`

Function · `agnoclaw.agent.get_current_run_context`

Return the active Agno run context during custom tool dispatch, else None.

```python
from agnoclaw import get_current_run_context

get_current_run_context() -> 'Any | None'
```

### `get_current_tool_runtime`

Function · `agnoclaw.agent.get_current_tool_runtime`

Return the currently executing tool runtime context, if any.

```python
from agnoclaw import get_current_tool_runtime

get_current_tool_runtime() -> 'dict[str, Any] | None'
```

### `HarnessAgent`

Class · `agnoclaw.agent.AgentHarness`

Embeddable Agno runtime for short, long, and controllable agent work.

```python
from agnoclaw import HarnessAgent

HarnessAgent(
    model: 'str | Model | AgnoModelFactory | None' = None,
    *,
    provider: 'str | None' = None,
    profile: 'str | RuntimeProfile | None' = None,
    cache_prompts: 'bool | None' = None,
    effort: 'str | None' = None,
    session_id: 'str | None' = None,
    user_id: 'str | None' = None,
    workspace_dir: 'str | Path | None' = None,
    allow_workspace_hooks: 'bool' = False,
    workspace_hook_env_allowlist: 'list[str] | tuple[str, ...] | None' = None,
    sandbox_dir: 'str | Path | None' = None,
    sandbox_mode: 'str | SandboxMode | None' = None,
    include_default_tools: 'bool' = True,
    tools: 'list | None' = None,
    capabilities: 'list | tuple | None' = None,
    capability_invoker: 'CapabilityInvoker | None' = None,
    instructions: 'str | None' = None,
    config: 'HarnessConfig | None' = None,
    db=None,
    name: 'str' = 'agnoclaw',
    agent_id: 'str | None' = None,
    debug: 'bool' = False,
    subagents: 'dict | None' = None,
    enable_user_memory: 'bool' = False,
    enable_learning: 'bool | None' = None,
    learning_mode: 'str | None' = None,
    learning_namespace: 'str | None' = None,
    learning_knowledge: 'Any | None' = None,
    enable_session_context: 'bool | None' = None,
    learning: 'LearningPolicy | None' = None,
    enable_compression: 'bool | None' = None,
    compress_token_limit: 'int | None' = None,
    enable_session_summary: 'bool | None' = None,
    num_history_runs: 'int | None' = None,
    num_history_messages: 'int | None' = None,
    max_tool_calls_from_history: 'int | None' = None,
    max_context_tokens: 'int | None' = None,
    auto_compact_context: 'bool | None' = None,
    context_lock_provider: 'ContextLockProvider | None' = None,
    max_inline_output_chars: 'int | None' = None,
    output_schema: 'type | dict[str, Any] | None' = None,
    parser_model: 'Any | None' = None,
    parser_model_prompt: 'str | None' = None,
    output_model: 'Any | None' = None,
    output_model_prompt: 'str | None' = None,
    parse_response: 'bool' = True,
    structured_outputs: 'bool | None' = None,
    use_json_mode: 'bool' = False,
    event_sink: 'EventSink | None' = None,
    event_sink_mode: 'str | None' = None,
    policy_engine: 'PolicyEngine | None' = None,
    policy_fail_open: 'bool | None' = None,
    permission_mode: 'str | None' = None,
    permission_approver: 'PermissionApprover | None' = None,
    permission_require_approver: 'bool | None' = None,
    permission_preapproved_tools: 'list[str] | tuple[str, ...] | None' = None,
    permission_preapproved_categories: 'list[str] | tuple[str, ...] | None' = None,
    backend: 'RuntimeBackend | None' = None,
    runtime_store: 'RuntimeStore | None' = None,
    artifact_store: 'ArtifactStore | None' = None,
    learning_ledger: 'LearningLedger | None' = None,
    learning_promotion_adapter: 'LearningPromotionAdapter | None' = None,
    skill_install_approver: 'SkillInstallApprover | None' = None,
    context_providers: 'list[ContextProvider] | tuple[ContextProvider, ...] | None' = None,
    dependencies: 'dict[str, Any] | None' = None,
    add_dependencies_to_context: 'bool' = False,
    session_state: 'dict[str, Any] | None' = None,
    add_session_state_to_context: 'bool' = False,
    packs: 'list[str | Path] | tuple[str | Path, ...] | None' = None,
    trusted_packs: 'bool' = False,
    pre_run_hooks: 'list[PreRunHook] | None' = None,
    post_run_hooks: 'list[PostRunHook] | None' = None,
    tenant_id: 'str | None' = None,
    org_id: 'str | None' = None,
    team_id: 'str | None' = None,
    roles: 'list[str] | tuple[str, ...] | None' = None,
    scopes: 'list[str] | tuple[str, ...] | None' = None,
    request_id: 'str | None' = None,
    trace_id: 'str | None' = None,
    context_metadata: 'dict[str, Any] | None' = None,
    skills_dirs: 'list[tuple[str | Path, str]] | None' = None,
    on_compaction: 'Callable[[str], Awaitable[None]] | None' = None,
    on_session_end: 'Callable[..., Awaitable[None] | None] | None' = None,
    session_metadata: 'dict[str, Any] | None' = None,
    model_id: 'str | None' = None,
    extra_tools: 'list | None' = None,
    extra_instructions: 'str | None' = None,
)
```

## `agnoclaw.backends`

### `RuntimeBackend`

Class · `agnoclaw.backends.RuntimeBackend`

Single runtime backend override for shell, files, skills, and browser tools.

```python
from agnoclaw import RuntimeBackend

RuntimeBackend(
    *,
    command_executor: 'CommandExecutor | None' = None,
    workspace_adapter: 'WorkspaceAdapter | None' = None,
    browser_backend: 'BrowserBackend | None' = None,
    sandbox_mode: 'str | SandboxMode | None' = None,
) -> 'None'
```

## `agnoclaw.capabilities`

### `CapabilityCatalogEntry`

Class · `agnoclaw.capabilities.CapabilityCatalogEntry`

CapabilityCatalogEntry(name: 'str', version: 'str', kind: 'CapabilityKind', effect_class: 'EffectClass', trust: 'CapabilityTrust', description: 'str', tags: 'tuple[str, ...]', digest: 'str')

```python
from agnoclaw import CapabilityCatalogEntry

CapabilityCatalogEntry(
    name: 'str',
    version: 'str',
    kind: 'CapabilityKind',
    effect_class: 'EffectClass',
    trust: 'CapabilityTrust',
    description: 'str',
    tags: 'tuple[str, ...]',
    digest: 'str',
) -> None
```

### `CapabilityConcurrency`

Class · `agnoclaw.capabilities.CapabilityConcurrency`

Enum where members are also (and must be) strings

```python
from agnoclaw import CapabilityConcurrency

CapabilityConcurrency(*values)
```

### `CapabilityKind`

Class · `agnoclaw.capabilities.CapabilityKind`

Enum where members are also (and must be) strings

```python
from agnoclaw import CapabilityKind

CapabilityKind(*values)
```

### `CapabilityLifetime`

Class · `agnoclaw.capabilities.CapabilityLifetime`

Enum where members are also (and must be) strings

```python
from agnoclaw import CapabilityLifetime

CapabilityLifetime(*values)
```

### `CapabilityRecovery`

Class · `agnoclaw.capabilities.CapabilityRecovery`

Enum where members are also (and must be) strings

```python
from agnoclaw import CapabilityRecovery

CapabilityRecovery(*values)
```

### `CapabilityRegistry`

Class · `agnoclaw.capabilities.CapabilityRegistry`

Copy-on-write registry with bounded discovery and explicit selection.

```python
from agnoclaw import CapabilityRegistry

CapabilityRegistry() -> 'None'
```

### `CapabilitySelection`

Class · `agnoclaw.capabilities.CapabilitySelection`

CapabilitySelection(specs: 'tuple[CapabilitySpec, ...]', schema_bytes: 'int', digest: 'str')

```python
from agnoclaw import CapabilitySelection

CapabilitySelection(specs: 'tuple[CapabilitySpec, ...]', schema_bytes: 'int', digest: 'str') -> None
```

### `CapabilitySpec`

Class · `agnoclaw.capabilities.CapabilitySpec`

Descriptor plus optional factory; only the descriptor enters persistence.

```python
from agnoclaw import CapabilitySpec

CapabilitySpec(
    name: 'str',
    version: 'str',
    kind: 'CapabilityKind',
    effect_class: 'EffectClass',
    trust: 'CapabilityTrust',
    lifetime: 'CapabilityLifetime',
    concurrency: 'CapabilityConcurrency',
    recovery: 'CapabilityRecovery',
    implementation_digest: 'str',
    description: 'str' = '',
    tags: 'tuple[str, ...]' = (),
    input_schema: 'Any' = <factory>,
    required_scopes: 'tuple[str, ...]' = (),
    supports_idempotency_key: 'bool' = False,
    factory: 'Callable[[], Any] | None' = None,
) -> None
```

### `CapabilityTrust`

Class · `agnoclaw.capabilities.CapabilityTrust`

Enum where members are also (and must be) strings

```python
from agnoclaw import CapabilityTrust

CapabilityTrust(*values)
```

## `agnoclaw.capability_execution`

### `CapabilityExecution`

Class · `agnoclaw.capability_execution.CapabilityExecution`

One selected capability and its authoritative operation settlement.

```python
from agnoclaw import CapabilityExecution

CapabilityExecution(spec: 'CapabilitySpec', operation: 'OperationExecution') -> None
```

### `CapabilityExecutor`

Class · `agnoclaw.capability_execution.CapabilityExecutor`

Small capability data plane over a registry and ``OperationGateway``.

```python
from agnoclaw import CapabilityExecutor

CapabilityExecutor(
    registry: 'CapabilityRegistry',
    gateway: 'OperationGateway',
    *,
    invoker: 'CapabilityInvoker | None' = None,
    max_argument_bytes: 'int' = 65536,
    max_metadata_bytes: 'int' = 4096,
) -> 'None'
```

## `agnoclaw.commands`

### `Fork`

Class · `agnoclaw.commands.Fork`

Request a new run from one certified source checkpoint.

```python
from agnoclaw import Fork

Fork(from_step: 'int | str', command_id: 'str' = <factory>) -> None
```

### `Pause`

Class · `agnoclaw.commands.Pause`

Request a pause at the next certified safe point.

```python
from agnoclaw import Pause

Pause(reason: 'str | None' = None, command_id: 'str' = <factory>) -> None
```

### `Respond`

Class · `agnoclaw.commands.Respond`

Supply input or an approval response to one exact pending request.

```python
from agnoclaw import Respond

Respond(request_id: 'str', payload: 'Any', command_id: 'str' = <factory>) -> None
```

### `Resume`

Class · `agnoclaw.commands.Resume`

Resume a paused run from its certified continuation boundary.

```python
from agnoclaw import Resume

Resume(command_id: 'str' = <factory>) -> None
```

### `Steer`

Class · `agnoclaw.commands.Steer`

Add operator guidance before a run closes its steering safe point.

```python
from agnoclaw import Steer

Steer(instruction: 'str', command_id: 'str' = <factory>) -> None
```

## `agnoclaw.compat`

### `AgnoCompatibilityReport`

Class · `agnoclaw.compat.AgnoCompatibilityReport`

AgnoCompatibilityReport(version: 'str', lane: 'AgnoLane', production_supported: 'bool', preview: 'bool', capabilities: 'tuple[CapabilityStatus, ...]')

```python
from agnoclaw import AgnoCompatibilityReport

AgnoCompatibilityReport(
    version: 'str',
    lane: 'AgnoLane',
    production_supported: 'bool',
    preview: 'bool',
    capabilities: 'tuple[CapabilityStatus, ...]',
) -> None
```

### `AgnoFeature`

Class · `agnoclaw.compat.AgnoFeature`

Enum where members are also (and must be) strings

```python
from agnoclaw import AgnoFeature

AgnoFeature(*values)
```

### `AgnoLane`

Class · `agnoclaw.compat.AgnoLane`

Enum where members are also (and must be) strings

```python
from agnoclaw import AgnoLane

AgnoLane(*values)
```

### `inspect_agno_compatibility`

Function · `agnoclaw.compat.inspect_agno_compatibility`

Inspect the actually installed Agno runtime and its importable contracts.

```python
from agnoclaw import inspect_agno_compatibility

inspect_agno_compatibility() -> 'AgnoCompatibilityReport'
```

### `require_supported_agno`

Function · `agnoclaw.compat.require_supported_agno`

Return the current compatibility report or raise for an unsupported Agno.

```python
from agnoclaw import require_supported_agno

require_supported_agno(*, allow_preview: 'bool' = False) -> 'AgnoCompatibilityReport'
```

## `agnoclaw.config`

### `get_config`

Function · `agnoclaw.config.get_config`

Load and cache the merged configuration.

```python
from agnoclaw import get_config

get_config() -> 'HarnessConfig'
```

### `HarnessConfig`

Class · `agnoclaw.config.HarnessConfig`

Base class for settings, allowing values to be overridden by environment variables.

```python
from agnoclaw import HarnessConfig

HarnessConfig(
    _case_sensitive: 'bool | None' = None,
    _nested_model_default_partial_update: 'bool | None' = None,
    _env_prefix: 'str | None' = None,
    _env_prefix_target: 'EnvPrefixTarget | None' = None,
    _env_file: 'DotenvType | None' = PosixPath('.'),
    _env_file_encoding: 'str | None' = None,
    _env_ignore_empty: 'bool | None' = None,
    _env_nested_delimiter: 'str | None' = None,
    _env_nested_max_split: 'int | None' = None,
    _env_parse_none_str: 'str | None' = None,
    _env_parse_enums: 'bool | None' = None,
    _cli_prog_name: 'str | None' = None,
    _cli_parse_args: 'bool | list[str] | tuple[str, ...] | None' = None,
    _cli_settings_source: 'CliSettingsSource[Any] | None' = None,
    _cli_parse_none_str: 'str | None' = None,
    _cli_hide_none_type: 'bool | None' = None,
    _cli_avoid_json: 'bool | None' = None,
    _cli_enforce_required: 'bool | None' = None,
    _cli_use_class_docs_for_groups: 'bool | None' = None,
    _cli_show_env_vars: 'bool | None' = None,
    _cli_exit_on_error: 'bool | None' = None,
    _cli_prefix: 'str | None' = None,
    _cli_flag_prefix_char: 'str | None' = None,
    _cli_implicit_flags: "bool | Literal['dual', 'toggle'] | None" = None,
    _cli_ignore_unknown_args: 'bool | None' = None,
    _cli_kebab_case: "bool | Literal['all', 'no_enums'] | None" = None,
    _cli_shortcuts: 'Mapping[str, str | list[str]] | None' = None,
    _secrets_dir: 'PathType | None' = None,
    _build_sources: 'tuple[tuple[PydanticBaseSettingsSource, ...], dict[str, Any]] | None' = None,
    *,
    profile: agnoclaw.config.RuntimeProfile = <RuntimeProfile.LEGACY: 'legacy'>,
    sandbox_mode: Optional[Literal['workspace_write', 'read_only', 'full']] = None,
    default_model: str = 'claude-sonnet-4-6',
    default_provider: str = 'anthropic',
    cache_prompts: bool = False,
    model_effort: str | None = None,
    workspace_dir: str = '~/.agnoclaw/workspace',
    session_history_runs: int = 10,
    runtime_max_concurrency: Annotated[int, Ge(ge=1), Le(le=10000)] = 16,
    runtime_max_waiting: Annotated[int, Ge(ge=1), Le(le=100000)] = 1024,
    runtime_max_waiting_per_tenant: Annotated[int, Ge(ge=1), Le(le=100000)] = 256,
    runtime_max_waiting_per_session: Annotated[int, Ge(ge=1), Le(le=100000)] = 32,
    runtime_admission_timeout_seconds: Annotated[float | None, Gt(gt=0), Le(le=3600)] = 30.0,
    runtime_close_policy: Literal['drain', 'detach', 'cancel'] = 'drain',
    runtime_close_timeout_seconds: Annotated[float | None, Gt(gt=0)] = None,
    runtime_operation_result_cache_size: Annotated[int, Ge(ge=0), Le(le=10000)] = 128,
    runtime_lease_seconds: Annotated[int, Ge(ge=3), Le(le=86400)] = 30,
    runtime_lease_renew_interval_seconds: Annotated[float, Gt(gt=0), Le(le=28800)] = 10.0,
    permission_durable_approvals: bool = True,
    permission_approval_ttl_seconds: Annotated[int, Ge(ge=1), Le(le=604800)] = 900,
    permission_approval_poll_interval_seconds: Annotated[float, Gt(gt=0), Le(le=5)] = 0.25,
    enable_bash: bool = True,
    enable_web_search: bool = True,
    enable_web_fetch: bool = True,
    bash_timeout_seconds: int = 120,
    enable_background_bash_tools: bool = False,
    skills_dirs: list[str] = <factory>,
    enable_learning: bool = False,
    learning_mode: str = 'agentic',
    enable_session_context: bool = False,
    enable_compression: bool = False,
    compress_token_limit: int | None = None,
    max_context_tokens: Annotated[int | None, Gt(gt=0)] = None,
    auto_compact_context: bool = False,
    max_inline_output_chars: Annotated[int | None, Ge(ge=1024), Le(le=1000000)] = None,
    enable_session_summary: bool = False,
    heartbeat: agnoclaw.config.HeartbeatConfig = <factory>,
    storage: agnoclaw.config.StorageConfig = <factory>,
    enable_browser: bool = False,
    mcp_servers: list[dict] = <factory>,
    enable_media_tools: bool = False,
    enable_notebook_tools: bool = False,
    enable_plugins: bool = True,
    plugin_paths: list[str] = <factory>,
    clawhub_url: str = 'https://clawhub.ai',
    clawhub_cache_dir: str = '~/.agnoclaw/cache/hub',
    global_workspace_dir: str = '~/.agnoclaw/global',
    project_workspace_dir: str = '.agnoclaw',
    theme: str = 'textual-dark',
    debug: bool = False,
    show_tool_calls: bool = False,
    event_sink_mode: str = 'best_effort',
    result_ref_keys: list[str] = <factory>,
    policy_fail_open: bool = False,
    guardrails_enabled: bool = True,
    path_guardrails_enabled: bool = True,
    path_allowed_roots: list[str] = <factory>,
    path_blocked_roots: list[str] = <factory>,
    network_enabled: bool = True,
    network_enforce_https: bool = True,
    network_allowed_hosts: list[str] = <factory>,
    network_blocked_hosts: list[str] = <factory>,
    network_block_private_hosts: bool = True,
    network_block_in_bash: bool = True,
    permission_mode: str = 'bypass',
    permission_require_approver: bool = False,
    permission_preapproved_tools: list[str] = <factory>,
    permission_preapproved_categories: list[str] = <factory>,
) -> None
```

### `RuntimeProfile`

Class · `agnoclaw.config.RuntimeProfile`

Runtime semantics selected independently from session continuity.

```python
from agnoclaw import RuntimeProfile

RuntimeProfile(*values)
```

## `agnoclaw.context_locking`

### `ContextLockLease`

Class · `agnoclaw.context_locking.ContextLockLease`

One exact-scope lock held until explicit idempotent release.

```python
from agnoclaw import ContextLockLease

ContextLockLease(*args, **kwargs)
```

### `ContextLockLostError`

Class · `agnoclaw.context_locking.ContextLockLostError`

The process can no longer prove ownership at the session write boundary.

```python
from agnoclaw import ContextLockLostError

ContextLockLostError(scope: 'ContextScope', *, mode: 'ContextLockMode') -> 'None'
```

### `ContextLockMode`

Class · `agnoclaw.context_locking.ContextLockMode`

Shared model activity or exclusive session replacement.

```python
from agnoclaw import ContextLockMode

ContextLockMode(*values)
```

### `ContextLockProvider`

Class · `agnoclaw.context_locking.ContextLockProvider`

Non-blocking context activity lock provider.

```python
from agnoclaw import ContextLockProvider

ContextLockProvider(*args, **kwargs)
```

### `ContextLockUnavailableError`

Class · `agnoclaw.context_locking.ContextLockUnavailableError`

Another cooperating process owns a conflicting session-context lock.

```python
from agnoclaw import ContextLockUnavailableError

ContextLockUnavailableError(scope: 'ContextScope', *, mode: 'ContextLockMode') -> 'None'
```

### `LocalFileContextLockProvider`

Class · `agnoclaw.context_locking.LocalFileContextLockProvider`

Crash-releasing reader/writer locks shared by local host processes.

```python
from agnoclaw import LocalFileContextLockProvider

LocalFileContextLockProvider(directory: 'str | Path') -> 'None'
```

## `agnoclaw.context_management`

### `ArchivedContextCheckpoint`

Class · `agnoclaw.context_management.ArchivedContextCheckpoint`

Content-free checkpoint evidence persisted in a session manifest.

```python
from agnoclaw import ArchivedContextCheckpoint

ArchivedContextCheckpoint(
    checkpoint_id: 'str',
    scope: 'ContextScope',
    sequence: 'int',
    segment_id: 'str',
    summary_digest: 'str',
    retained_item_ids: 'tuple[str, ...]',
    before_tokens: 'int',
    after_tokens: 'int',
) -> None
```

### `ArchivedContextSegment`

Class · `agnoclaw.context_management.ArchivedContextSegment`

Content-free manifest entry for one archived segment.

```python
from agnoclaw import ArchivedContextSegment

ArchivedContextSegment(
    segment_id: 'str',
    scope: 'ContextScope',
    sequence: 'int',
    artifact: 'ArtifactReference',
    total_tokens: 'int',
    source_tokens: 'int',
    item_ids: 'tuple[str, ...]',
) -> None
```

### `ArtifactContextArchive`

Class · `agnoclaw.context_management.ArtifactContextArchive`

Bounded context archive over an injected scoped ArtifactStore.

```python
from agnoclaw import ArtifactContextArchive

ArtifactContextArchive(
    store: 'ArtifactStore',
    *,
    max_items_per_segment: 'int' = 20000,
    max_item_bytes: 'int' = 1000000,
    max_search_segments: 'int' = 1000,
    max_rehydrate_tokens: 'int' = 16000,
    counter: 'TokenCounter | None' = None,
) -> 'None'
```

### `ContextBudget`

Class · `agnoclaw.context_management.ContextBudget`

One inspectable context-budget measurement with hysteresis thresholds.

```python
from agnoclaw import ContextBudget

ContextBudget(
    used_tokens: 'int',
    max_tokens: 'int',
    prepare_at: 'float' = 0.8,
    compact_at: 'float' = 0.9,
    emergency_at: 'float' = 0.97,
    release_below: 'float' = 0.7,
    exact: 'bool' = False,
) -> None
```

### `ContextBudgetAction`

Class · `agnoclaw.context_management.ContextBudgetAction`

Truthful budget recommendation; it never triggers mutation by itself.

```python
from agnoclaw import ContextBudgetAction

ContextBudgetAction(*values)
```

### `ContextCheckpoint`

Class · `agnoclaw.context_management.ContextCheckpoint`

Evidence that one archived segment replaced a bounded live context.

```python
from agnoclaw import ContextCheckpoint

ContextCheckpoint(
    checkpoint_id: 'str',
    scope: 'ContextScope',
    sequence: 'int',
    segment_id: 'str',
    summary: 'str',
    retained_item_ids: 'tuple[str, ...]',
    before_tokens: 'int',
    after_tokens: 'int',
) -> None
```

### `ContextContinuationRecord`

Class · `agnoclaw.context_management.ContextContinuationRecord`

Typed, bounded state that must survive a context replacement.

```python
from agnoclaw import ContextContinuationRecord

ContextContinuationRecord(
    summary: 'str',
    goal: 'str | None' = None,
    plan: 'tuple[str, ...]' = (),
    progress: 'tuple[str, ...]' = (),
    decisions: 'tuple[str, ...]' = (),
    approvals: 'tuple[str, ...]' = (),
    open_questions: 'tuple[str, ...]' = (),
    tests: 'tuple[str, ...]' = (),
    files: 'tuple[str, ...]' = (),
    citations: 'tuple[str, ...]' = (),
) -> None
```

### `ContextItem`

Class · `agnoclaw.context_management.ContextItem`

Stable itemized context; content stays in its scoped trajectory artifact.

```python
from agnoclaw import ContextItem

ContextItem(
    item_id: 'str',
    kind: 'ContextItemKind',
    content: 'str',
    ordinal: 'int',
    token_count: 'int',
    invariant: 'bool' = False,
    provenance: 'Any' = <factory>,
) -> None
```

### `ContextItemKind`

Class · `agnoclaw.context_management.ContextItemKind`

Semantic class used by retention and inspection policies.

```python
from agnoclaw import ContextItemKind

ContextItemKind(*values)
```

### `ContextItemNotFoundError`

Class · `agnoclaw.context_management.ContextItemNotFoundError`

Stable error shape for public runtime operations.

```python
from agnoclaw import ContextItemNotFoundError

ContextItemNotFoundError(item_ids: 'Iterable[str]') -> 'None'
```

### `ContextManifest`

Class · `agnoclaw.context_management.ContextManifest`

Bounded, content-free chain persisted with the owning Agno session.

```python
from agnoclaw import ContextManifest

ContextManifest(
    scope: 'ContextScope',
    revision: 'int' = 0,
    segments: 'tuple[ArchivedContextSegment, ...]' = (),
    checkpoints: 'tuple[ArchivedContextCheckpoint, ...]' = (),
    schema_version: 'str' = '1.0',
) -> None
```

### `ContextManifestConflictError`

Class · `agnoclaw.context_management.ContextManifestConflictError`

Stable error shape for public runtime operations.

```python
from agnoclaw import ContextManifestConflictError

ContextManifestConflictError(*, expected: 'int') -> 'None'
```

### `ContextManifestLimitError`

Class · `agnoclaw.context_management.ContextManifestLimitError`

Stable error shape for public runtime operations.

```python
from agnoclaw import ContextManifestLimitError

ContextManifestLimitError(*, max_segments: 'int') -> 'None'
```

### `ContextQualityError`

Class · `agnoclaw.context_management.ContextQualityError`

Stable error shape for public runtime operations.

```python
from agnoclaw import ContextQualityError

ContextQualityError(*, reason: 'str', details: 'Mapping[str, Any] | None' = None) -> 'None'
```

### `ContextRehydration`

Class · `agnoclaw.context_management.ContextRehydration`

Exact archived items selected for return or reinsertion into live context.

```python
from agnoclaw import ContextRehydration

ContextRehydration(
    scope: 'ContextScope',
    items: 'tuple[ContextItem, ...]',
    sources: 'tuple[ContextSource, ...]' = (<ContextSource.TRAJECTORY: 'trajectory'>, <ContextSource.ARTIFACT: 'artifact'>),
    injected: 'bool' = False,
) -> None
```

### `ContextScope`

Class · `agnoclaw.context_management.ContextScope`

Exact tenant/user/session namespace for context material.

```python
from agnoclaw import ContextScope

ContextScope(session_id: 'str', tenant_id: 'str | None' = None, user_id: 'str | None' = None) -> None
```

### `ContextScopeError`

Class · `agnoclaw.context_management.ContextScopeError`

Stable error shape for public runtime operations.

```python
from agnoclaw import ContextScopeError

ContextScopeError(expected: 'ContextScope', *, actual: 'ContextScope') -> 'None'
```

### `ContextSearchHit`

Class · `agnoclaw.context_management.ContextSearchHit`

One scoped search result with explicit reconstruction provenance.

```python
from agnoclaw import ContextSearchHit

ContextSearchHit(
    item_id: 'str',
    segment_id: 'str',
    kind: 'ContextItemKind',
    excerpt: 'str',
    score: 'float',
    source: 'ContextSource',
    provenance: 'Any',
) -> None
```

### `ContextSource`

Class · `agnoclaw.context_management.ContextSource`

Where context supplied to a caller or model was reconstructed from.

```python
from agnoclaw import ContextSource

ContextSource(*values)
```

### `DeterministicTokenCounter`

Class · `agnoclaw.context_management.DeterministicTokenCounter`

Conservative deterministic fallback when a model tokenizer is unavailable.

```python
from agnoclaw import DeterministicTokenCounter

DeterministicTokenCounter(utf8_bytes_per_token: 'int' = 4) -> None
```

### `TokenCounter`

Class · `agnoclaw.context_management.TokenCounter`

Small provider-neutral token accounting seam.

```python
from agnoclaw import TokenCounter

TokenCounter(*args, **kwargs)
```

## `agnoclaw.context_providers`

### `context_provider_capability`

Function · `agnoclaw.context_providers.context_provider_capability`

Expose one Agno provider's read query through the durable capability kernel.

```python
from agnoclaw import context_provider_capability

context_provider_capability(
    provider_id: 'str',
    factory: 'Callable[[], ContextProvider]',
    *,
    version: 'str',
    implementation_digest: 'str',
    effect_class: 'EffectClass',
    description: 'str | None' = None,
    required_scopes: 'Sequence[str]' = (),
    trust: 'CapabilityTrust' = <CapabilityTrust.HOST_MANAGED: 'host_managed'>,
    maximum_question_chars: 'int' = 16384,
    maximum_answer_bytes: 'int' = 4194304,
) -> 'CapabilitySpec'
```

## `agnoclaw.improvement`

### `ChangeBudget`

Class · `agnoclaw.improvement.ChangeBudget`

ChangeBudget(max_rollouts: 'int', max_tokens: 'int', max_wall_seconds: 'float', max_cost_usd: 'float')

```python
from agnoclaw import ChangeBudget

ChangeBudget(
    max_rollouts: 'int',
    max_tokens: 'int',
    max_wall_seconds: 'float',
    max_cost_usd: 'float',
) -> None
```

### `ChangeHypothesis`

Class · `agnoclaw.improvement.ChangeHypothesis`

ChangeHypothesis(change_id: 'str', target_component_ids: 'tuple[str, ...]', component_manifest_digests: 'tuple[str, ...]', failure_cluster_ids: 'tuple[str, ...]', failure_cluster_digests: 'tuple[str, ...]', evidence_artifact_ids: 'tuple[str, ...]', inferred_root_cause: 'str', bounded_edit_surface: 'tuple[str, ...]', predicted_fixes: 'tuple[str, ...]', at_risk_regressions: 'tuple[str, ...]', behaviors_to_preserve: 'tuple[str, ...]', previous_attempt_ids: 'tuple[str, ...]', model_config_digest: 'str', evaluator_digest: 'str', permission_digest: 'str', proposer_identity_digest: 'str', budget: 'ChangeBudget', rollback_target: 'str', proposed_by: 'ImprovementRole', mechanism_version: 'str')

```python
from agnoclaw import ChangeHypothesis

ChangeHypothesis(
    change_id: 'str',
    target_component_ids: 'tuple[str, ...]',
    component_manifest_digests: 'tuple[str, ...]',
    failure_cluster_ids: 'tuple[str, ...]',
    failure_cluster_digests: 'tuple[str, ...]',
    evidence_artifact_ids: 'tuple[str, ...]',
    inferred_root_cause: 'str',
    bounded_edit_surface: 'tuple[str, ...]',
    predicted_fixes: 'tuple[str, ...]',
    at_risk_regressions: 'tuple[str, ...]',
    behaviors_to_preserve: 'tuple[str, ...]',
    previous_attempt_ids: 'tuple[str, ...]',
    model_config_digest: 'str',
    evaluator_digest: 'str',
    permission_digest: 'str',
    proposer_identity_digest: 'str',
    budget: 'ChangeBudget',
    rollback_target: 'str',
    proposed_by: 'ImprovementRole',
    mechanism_version: 'str',
) -> None
```

### `EvaluationGateDecision`

Class · `agnoclaw.improvement.EvaluationGateDecision`

EvaluationGateDecision(candidate_id: 'str', qualified: 'bool', verdict: 'EvaluationVerdict', reasons: 'tuple[str, ...]', deltas: 'Any', baseline_metrics: 'Any', candidate_metrics: 'Any', objective: 'ObjectiveVector', evidence_artifact_ids: 'tuple[str, ...]', hypothesis_digest: 'str', policy_digest: 'str', evaluator_digest: 'str', evaluation_digest: 'str', runner_digest: 'str | None' = None, corpus_manifest_digest: 'str | None' = None)

```python
from agnoclaw import EvaluationGateDecision

EvaluationGateDecision(
    candidate_id: 'str',
    qualified: 'bool',
    verdict: 'EvaluationVerdict',
    reasons: 'tuple[str, ...]',
    deltas: 'Any',
    baseline_metrics: 'Any',
    candidate_metrics: 'Any',
    objective: 'ObjectiveVector',
    evidence_artifact_ids: 'tuple[str, ...]',
    hypothesis_digest: 'str',
    policy_digest: 'str',
    evaluator_digest: 'str',
    evaluation_digest: 'str',
    runner_digest: 'str | None' = None,
    corpus_manifest_digest: 'str | None' = None,
) -> None
```

### `EvaluationGatePolicy`

Class · `agnoclaw.improvement.EvaluationGatePolicy`

EvaluationGatePolicy(min_held_in_samples: 'int' = 20, min_held_out_samples: 'int' = 20, min_transfer_samples: 'int' = 10, min_held_in_quality_delta: 'float' = 0.01, max_held_out_quality_regression: 'float' = 0.0, max_transfer_quality_regression: 'float' = 0.0, max_safety_regression: 'float' = 0.0, max_cost_ratio: 'float' = 1.5, max_latency_ratio: 'float' = 1.5, min_objective_fraction: 'float' = 0.5, min_novelty_score: 'float' = 0.1, min_diversity_score: 'float' = 0.1, max_judge_disagreement_rate: 'float' = 0.2, require_governed_corpus: 'bool' = True)

```python
from agnoclaw import EvaluationGatePolicy

EvaluationGatePolicy(
    min_held_in_samples: 'int' = 20,
    min_held_out_samples: 'int' = 20,
    min_transfer_samples: 'int' = 10,
    min_held_in_quality_delta: 'float' = 0.01,
    max_held_out_quality_regression: 'float' = 0.0,
    max_transfer_quality_regression: 'float' = 0.0,
    max_safety_regression: 'float' = 0.0,
    max_cost_ratio: 'float' = 1.5,
    max_latency_ratio: 'float' = 1.5,
    min_objective_fraction: 'float' = 0.5,
    min_novelty_score: 'float' = 0.1,
    min_diversity_score: 'float' = 0.1,
    max_judge_disagreement_rate: 'float' = 0.2,
    require_governed_corpus: 'bool' = True,
) -> None
```

### `EvaluationResourceUsage`

Class · `agnoclaw.improvement.EvaluationResourceUsage`

EvaluationResourceUsage(rollouts: 'int', tokens: 'int', wall_seconds: 'float', cost_usd: 'float')

```python
from agnoclaw import EvaluationResourceUsage

EvaluationResourceUsage(rollouts: 'int', tokens: 'int', wall_seconds: 'float', cost_usd: 'float') -> None
```

### `EvaluationSlice`

Class · `agnoclaw.improvement.EvaluationSlice`

Enum where members are also (and must be) strings

```python
from agnoclaw import EvaluationSlice

EvaluationSlice(*values)
```

### `EvaluationSliceResult`

Class · `agnoclaw.improvement.EvaluationSliceResult`

EvaluationSliceResult(slice: 'EvaluationSlice', task_class: 'str', dataset_digest: 'str', verifier_digest: 'str', sample_count: 'int', quality: 'float', safety: 'float', cost_usd: 'float', latency_seconds: 'float', objective_fraction: 'float', evidence_artifact_ids: 'tuple[str, ...]')

```python
from agnoclaw import EvaluationSliceResult

EvaluationSliceResult(
    slice: 'EvaluationSlice',
    task_class: 'str',
    dataset_digest: 'str',
    verifier_digest: 'str',
    sample_count: 'int',
    quality: 'float',
    safety: 'float',
    cost_usd: 'float',
    latency_seconds: 'float',
    objective_fraction: 'float',
    evidence_artifact_ids: 'tuple[str, ...]',
) -> None
```

### `FailureCluster`

Class · `agnoclaw.improvement.FailureCluster`

FailureCluster(cluster_id: 'str', causal_mechanism: 'str', verifier_digest: 'str', failure_run_ids: 'tuple[str, ...]', evidence_artifact_ids: 'tuple[str, ...]', terminal_labels: 'tuple[str, ...]', mechanism_version: 'str')

```python
from agnoclaw import FailureCluster

FailureCluster(
    cluster_id: 'str',
    causal_mechanism: 'str',
    verifier_digest: 'str',
    failure_run_ids: 'tuple[str, ...]',
    evidence_artifact_ids: 'tuple[str, ...]',
    terminal_labels: 'tuple[str, ...]',
    mechanism_version: 'str',
) -> None
```

### `FeedbackStrength`

Class · `agnoclaw.improvement.FeedbackStrength`

Enum where members are also (and must be) strings

```python
from agnoclaw import FeedbackStrength

FeedbackStrength(*values)
```

### `HarnessComponentClass`

Class · `agnoclaw.improvement.HarnessComponentClass`

Enum where members are also (and must be) strings

```python
from agnoclaw import HarnessComponentClass

HarnessComponentClass(*values)
```

### `HarnessComponentManifest`

Class · `agnoclaw.improvement.HarnessComponentManifest`

HarnessComponentManifest(component_id: 'str', component_class: 'HarnessComponentClass', version: 'str', implementation_digest: 'str', editable_paths: 'tuple[str, ...]', rollback_reference: 'str', description: 'str', metadata: 'Any' = <factory>, schema_version: 'str' = '1.0')

```python
from agnoclaw import HarnessComponentManifest

HarnessComponentManifest(
    component_id: 'str',
    component_class: 'HarnessComponentClass',
    version: 'str',
    implementation_digest: 'str',
    editable_paths: 'tuple[str, ...]',
    rollback_reference: 'str',
    description: 'str',
    metadata: 'Any' = <factory>,
    schema_version: 'str' = '1.0',
) -> None
```

### `ImprovementEvaluation`

Class · `agnoclaw.improvement.ImprovementEvaluation`

ImprovementEvaluation(candidate_id: 'str', hypothesis_digest: 'str', baseline: 'tuple[EvaluationSliceResult, ...]', candidate: 'tuple[EvaluationSliceResult, ...]', evaluator_digest: 'str', evaluator_identity_digest: 'str', model_config_digest: 'str', permission_digest: 'str', feedback_strength: 'FeedbackStrength', usage: 'EvaluationResourceUsage', safety_passed: 'bool', privacy_passed: 'bool', novelty_score: 'float', diversity_score: 'float', added_complexity: 'float', judge_audit: 'JudgeAudit' = <factory>, paired_statistics: 'tuple[PairedQualityStatistic, ...]' = (), runner_digest: 'str | None' = None, baseline_subject_contract_digest: 'str | None' = None, candidate_subject_contract_digest: 'str | None' = None, subject_isolation_digest: 'str | None' = None, corpus_manifest_digest: 'str | None' = None, corpus_evidence_artifact_ids: 'tuple[str, ...]' = ())

```python
from agnoclaw import ImprovementEvaluation

ImprovementEvaluation(
    candidate_id: 'str',
    hypothesis_digest: 'str',
    baseline: 'tuple[EvaluationSliceResult, ...]',
    candidate: 'tuple[EvaluationSliceResult, ...]',
    evaluator_digest: 'str',
    evaluator_identity_digest: 'str',
    model_config_digest: 'str',
    permission_digest: 'str',
    feedback_strength: 'FeedbackStrength',
    usage: 'EvaluationResourceUsage',
    safety_passed: 'bool',
    privacy_passed: 'bool',
    novelty_score: 'float',
    diversity_score: 'float',
    added_complexity: 'float',
    judge_audit: 'JudgeAudit' = <factory>,
    paired_statistics: 'tuple[PairedQualityStatistic, ...]' = (),
    runner_digest: 'str | None' = None,
    baseline_subject_contract_digest: 'str | None' = None,
    candidate_subject_contract_digest: 'str | None' = None,
    subject_isolation_digest: 'str | None' = None,
    corpus_manifest_digest: 'str | None' = None,
    corpus_evidence_artifact_ids: 'tuple[str, ...]' = (),
) -> None
```

### `ImprovementEvaluationGate`

Class · `agnoclaw.improvement.ImprovementEvaluationGate`

Pure, fail-closed held-in/held-out/transfer acceptance gate.

```python
from agnoclaw import ImprovementEvaluationGate

ImprovementEvaluationGate(policy: 'EvaluationGatePolicy | None' = None) -> 'None'
```

### `ImprovementRole`

Class · `agnoclaw.improvement.ImprovementRole`

Enum where members are also (and must be) strings

```python
from agnoclaw import ImprovementRole

ImprovementRole(*values)
```

### `JudgeAudit`

Class · `agnoclaw.improvement.JudgeAudit`

JudgeAudit(used: 'bool' = False, balanced_order: 'bool' = False, disagreement_rate: 'float' = 0.0, disagreements_reviewed: 'bool' = False, judge_model_digest: 'str | None' = None, judge_prompt_digest: 'str | None' = None, calibration_artifact_ids: 'tuple[str, ...]' = ())

```python
from agnoclaw import JudgeAudit

JudgeAudit(
    used: 'bool' = False,
    balanced_order: 'bool' = False,
    disagreement_rate: 'float' = 0.0,
    disagreements_reviewed: 'bool' = False,
    judge_model_digest: 'str | None' = None,
    judge_prompt_digest: 'str | None' = None,
    calibration_artifact_ids: 'tuple[str, ...]' = (),
) -> None
```

### `ObjectiveVector`

Class · `agnoclaw.improvement.ObjectiveVector`

ObjectiveVector(quality: 'float', safety: 'float', cost_usd: 'float', latency_seconds: 'float', complexity: 'float')

```python
from agnoclaw import ObjectiveVector

ObjectiveVector(
    quality: 'float',
    safety: 'float',
    cost_usd: 'float',
    latency_seconds: 'float',
    complexity: 'float',
) -> None
```

### `PairedQualityStatistic`

Class · `agnoclaw.improvement.PairedQualityStatistic`

Paired quality-delta confidence evidence for one frozen evaluation slice.

```python
from agnoclaw import PairedQualityStatistic

PairedQualityStatistic(
    slice: 'EvaluationSlice',
    sample_count: 'int',
    mean_delta: 'float',
    confidence_low: 'float',
    confidence_high: 'float',
    wins: 'int',
    ties: 'int',
    losses: 'int',
    confidence_level: 'float' = 0.95,
    method: 'str' = 'paired_t_95',
) -> None
```

### `pareto_frontier`

Function · `agnoclaw.improvement.pareto_frontier`

Return deterministic non-dominated entries without scalarizing objectives.

```python
from agnoclaw import pareto_frontier

pareto_frontier(entries: 'Iterable[ParetoEntry]') -> 'tuple[ParetoEntry, ...]'
```

### `ParetoEntry`

Class · `agnoclaw.improvement.ParetoEntry`

ParetoEntry(candidate_id: 'str', hypothesis_digest: 'str', objective: 'ObjectiveVector', evaluation_digest: 'str')

```python
from agnoclaw import ParetoEntry

ParetoEntry(
    candidate_id: 'str',
    hypothesis_digest: 'str',
    objective: 'ObjectiveVector',
    evaluation_digest: 'str',
) -> None
```

## `agnoclaw.improvement_agno`

### `agno_evaluation_subject_factory`

Function · `agnoclaw.improvement_agno.agno_evaluation_subject_factory`

Return a runner factory that asks the host for one fresh Agent per rollout.

```python
from agnoclaw import agno_evaluation_subject_factory

agno_evaluation_subject_factory(
    agent_factory: 'Callable[[], Any]',
    *,
    input_builder: 'EvaluationInputBuilder' = <function _default_input>,
    output_builder: 'EvaluationOutputBuilder' = <function _default_output>,
    session_prefix: 'str' = 'agnoclaw-improvement-eval',
    close_agent: 'bool' = False,
) -> 'SubjectFactory'
```

### `AgnoEvaluationSubject`

Class · `agnoclaw.improvement_agno.AgnoEvaluationSubject`

One fresh Agno Agent adapted to the runner's async subject contract.

```python
from agnoclaw import AgnoEvaluationSubject

AgnoEvaluationSubject(
    agent: 'Any',
    *,
    input_builder: 'EvaluationInputBuilder' = <function _default_input>,
    output_builder: 'EvaluationOutputBuilder' = <function _default_output>,
    session_prefix: 'str' = 'agnoclaw-improvement-eval',
    close_agent: 'bool' = False,
) -> 'None'
```

## `agnoclaw.improvement_corpus`

### `evaluation_corpus_case_set_digest`

Function · `agnoclaw.improvement_corpus.evaluation_corpus_case_set_digest`

Digest an ordered, content-free corpus entry set before staging its audit.

```python
from agnoclaw import evaluation_corpus_case_set_digest

evaluation_corpus_case_set_digest(entries: 'Iterable[EvaluationCorpusEntry]') -> 'str'
```

### `EvaluationCase`

Class · `agnoclaw.improvement_corpus.EvaluationCase`

One immutable case in exactly one frozen evaluation slice.

```python
from agnoclaw import EvaluationCase

EvaluationCase(case_id: 'str', slice: 'EvaluationSlice', task_class: 'str', payload: 'Any') -> None
```

### `EvaluationCaseExposure`

Class · `agnoclaw.improvement_corpus.EvaluationCaseExposure`

Enum where members are also (and must be) strings

```python
from agnoclaw import EvaluationCaseExposure

EvaluationCaseExposure(*values)
```

### `EvaluationCorpusEntry`

Class · `agnoclaw.improvement_corpus.EvaluationCorpusEntry`

Content-free case identity, provenance, split, and semantic lineage.

```python
from agnoclaw import EvaluationCorpusEntry

EvaluationCorpusEntry(
    case_id: 'str',
    slice: 'EvaluationSlice',
    task_class: 'str',
    payload_digest: 'str',
    lineage_digest: 'str',
    source_artifact_id: 'str',
    exposure: 'EvaluationCaseExposure',
) -> None
```

### `EvaluationCorpusManifest`

Class · `agnoclaw.improvement_corpus.EvaluationCorpusManifest`

Frozen corpus membership plus independently retained contamination evidence.

```python
from agnoclaw import EvaluationCorpusManifest

EvaluationCorpusManifest(
    corpus_id: 'str',
    version: 'str',
    entries: 'tuple[EvaluationCorpusEntry, ...]',
    selection_policy_digest: 'str',
    sampling_seed_digest: 'str',
    sealed_access_policy_digest: 'str',
    decontamination_method_digest: 'str',
    decontamination_artifact_id: 'str',
    curator_identity_digest: 'str',
    schema_version: 'str' = '1.0',
) -> None
```

### `EvaluationCorpusUsageBasis`

Class · `agnoclaw.improvement_corpus.EvaluationCorpusUsageBasis`

Enum where members are also (and must be) strings

```python
from agnoclaw import EvaluationCorpusUsageBasis

EvaluationCorpusUsageBasis(*values)
```

## `agnoclaw.improvement_docker`

### `docker_evaluation_subject_factory`

Function · `agnoclaw.improvement_docker.docker_evaluation_subject_factory`

Build a strict no-network Docker subject factory.

```python
from agnoclaw import docker_evaluation_subject_factory

docker_evaluation_subject_factory(
    docker_executable: 'str | os.PathLike[str]',
    policy: 'DockerEvaluationPolicy',
    container_command: 'Sequence[str]',
    *,
    max_request_bytes: 'int' = 1048576,
    max_stdout_bytes: 'int' = 1048576,
    max_stderr_bytes: 'int' = 65536,
    terminate_grace_seconds: 'float' = 1.0,
    cleanup_timeout_seconds: 'float' = 5.0,
) -> 'DockerEvaluationSubjectFactory'
```

### `DockerEvaluationImageError`

Class · `agnoclaw.improvement_docker.DockerEvaluationImageError`

The immutable image cannot satisfy the policy preflight.

```python
from agnoclaw import DockerEvaluationImageError

DockerEvaluationImageError(...)
```

### `DockerEvaluationPolicy`

Class · `agnoclaw.improvement_docker.DockerEvaluationPolicy`

Immutable, no-network container policy shared by both sides of an experiment.

```python
from agnoclaw import DockerEvaluationPolicy

DockerEvaluationPolicy(
    image: 'str',
    platform: 'str' = 'linux/amd64',
    memory_bytes: 'int' = 536870912,
    cpu_limit: 'float' = 1.0,
    pids_limit: 'int' = 64,
    tmpfs_bytes: 'int' = 67108864,
    nofile_limit: 'int' = 1024,
    user: 'str' = '65532:65532',
) -> None
```

### `DockerEvaluationSubject`

Class · `agnoclaw.improvement_docker.DockerEvaluationSubject`

One single-use, exactly cleaned Docker rollout subject.

```python
from agnoclaw import DockerEvaluationSubject

DockerEvaluationSubject(config: '_DockerEvaluationConfig') -> 'None'
```

### `DockerEvaluationSubjectFactory`

Class · `agnoclaw.improvement_docker.DockerEvaluationSubjectFactory`

Redacted callable producing one hardened container per rollout.

```python
from agnoclaw import DockerEvaluationSubjectFactory

DockerEvaluationSubjectFactory(
    docker_executable: 'str | os.PathLike[str]',
    policy: 'DockerEvaluationPolicy',
    container_command: 'Sequence[str]',
    *,
    max_request_bytes: 'int' = 1048576,
    max_stdout_bytes: 'int' = 1048576,
    max_stderr_bytes: 'int' = 65536,
    terminate_grace_seconds: 'float' = 1.0,
    cleanup_timeout_seconds: 'float' = 5.0,
) -> 'None'
```

## `agnoclaw.improvement_process`

### `process_evaluation_subject_factory`

Function · `agnoclaw.improvement_process.process_evaluation_subject_factory`

Build a redacted factory for one fresh child process per runner rollout.

```python
from agnoclaw import process_evaluation_subject_factory

process_evaluation_subject_factory(
    command: 'Sequence[str]',
    *,
    environment: 'Mapping[str, str] | None' = None,
    working_directory: 'str | os.PathLike[str] | None' = None,
    max_request_bytes: 'int' = 1048576,
    max_stdout_bytes: 'int' = 1048576,
    max_stderr_bytes: 'int' = 65536,
    terminate_grace_seconds: 'float' = 1.0,
) -> 'ProcessEvaluationSubjectFactory'
```

### `ProcessEvaluationCleanupError`

Class · `agnoclaw.improvement_process.ProcessEvaluationCleanupError`

A child or owned working directory could not be deterministically released.

```python
from agnoclaw import ProcessEvaluationCleanupError

ProcessEvaluationCleanupError(...)
```

### `ProcessEvaluationError`

Class · `agnoclaw.improvement_process.ProcessEvaluationError`

Base class whose messages never include child output or environment values.

```python
from agnoclaw import ProcessEvaluationError

ProcessEvaluationError(...)
```

### `ProcessEvaluationExitedError`

Class · `agnoclaw.improvement_process.ProcessEvaluationExitedError`

The child exited without a successful protocol response.

```python
from agnoclaw import ProcessEvaluationExitedError

ProcessEvaluationExitedError(returncode: 'int') -> 'None'
```

### `ProcessEvaluationOutputLimitError`

Class · `agnoclaw.improvement_process.ProcessEvaluationOutputLimitError`

A child stream crossed its configured byte boundary.

```python
from agnoclaw import ProcessEvaluationOutputLimitError

ProcessEvaluationOutputLimitError(stream: 'str') -> 'None'
```

### `ProcessEvaluationProtocolError`

Class · `agnoclaw.improvement_process.ProcessEvaluationProtocolError`

The child response did not satisfy the exact protocol contract.

```python
from agnoclaw import ProcessEvaluationProtocolError

ProcessEvaluationProtocolError(...)
```

### `ProcessEvaluationRemoteError`

Class · `agnoclaw.improvement_process.ProcessEvaluationRemoteError`

The worker reported a bounded error type without an error message.

```python
from agnoclaw import ProcessEvaluationRemoteError

ProcessEvaluationRemoteError(remote_error_type: 'str') -> 'None'
```

### `ProcessEvaluationRequestLimitError`

Class · `agnoclaw.improvement_process.ProcessEvaluationRequestLimitError`

The canonical case request exceeded the configured process boundary.

```python
from agnoclaw import ProcessEvaluationRequestLimitError

ProcessEvaluationRequestLimitError(...)
```

### `ProcessEvaluationSubject`

Class · `agnoclaw.improvement_process.ProcessEvaluationSubject`

Execute one case in one fresh child process using a bounded JSON protocol.

```python
from agnoclaw import ProcessEvaluationSubject

ProcessEvaluationSubject(
    command: 'Sequence[str]',
    *,
    environment: 'Mapping[str, str] | None' = None,
    working_directory: 'str | os.PathLike[str] | None' = None,
    max_request_bytes: 'int' = 1048576,
    max_stdout_bytes: 'int' = 1048576,
    max_stderr_bytes: 'int' = 65536,
    terminate_grace_seconds: 'float' = 1.0,
    _config: '_ProcessEvaluationConfig | None' = None,
) -> 'None'
```

### `ProcessEvaluationSubjectFactory`

Class · `agnoclaw.improvement_process.ProcessEvaluationSubjectFactory`

Validated, redacted callable that creates one configured subject per rollout.

```python
from agnoclaw import ProcessEvaluationSubjectFactory

ProcessEvaluationSubjectFactory(
    command: 'Sequence[str]',
    *,
    environment: 'Mapping[str, str] | None' = None,
    working_directory: 'str | os.PathLike[str] | None' = None,
    max_request_bytes: 'int' = 1048576,
    max_stdout_bytes: 'int' = 1048576,
    max_stderr_bytes: 'int' = 65536,
    terminate_grace_seconds: 'float' = 1.0,
) -> 'None'
```

### `run_process_evaluation_worker`

Function · `agnoclaw.improvement_process.run_process_evaluation_worker`

Run one bounded worker request from stdin and emit one exact JSON response.

```python
from agnoclaw import run_process_evaluation_worker

run_process_evaluation_worker(handler: 'ProcessEvaluationHandler', *, max_request_bytes: 'int' = 1048576) -> 'int'
```

## `agnoclaw.improvement_runner`

### `EvaluationRollout`

Class · `agnoclaw.improvement_runner.EvaluationRollout`

A subject result plus independently metered usage.

```python
from agnoclaw import EvaluationRollout

EvaluationRollout(output: 'Any', tokens: 'int' = 0, cost_usd: 'float' = 0.0) -> None
```

### `EvaluationScore`

Class · `agnoclaw.improvement_runner.EvaluationScore`

Independent verifier result for one rollout.

```python
from agnoclaw import EvaluationScore

EvaluationScore(
    quality: 'float',
    safety: 'float',
    safety_passed: 'bool',
    privacy_passed: 'bool',
    objective: 'bool' = True,
) -> None
```

### `ImprovementEvaluationRun`

Class · `agnoclaw.improvement_runner.ImprovementEvaluationRun`

Runner report plus exact verified and generated artifact references.

```python
from agnoclaw import ImprovementEvaluationRun

ImprovementEvaluationRun(
    report: 'ImprovementEvaluation',
    upstream_artifacts: 'tuple[ArtifactReference, ...]',
    case_artifacts: 'tuple[ArtifactReference, ...]',
    execution_order_balanced: 'bool',
) -> None
```

### `ImprovementEvaluationRunner`

Class · `agnoclaw.improvement_runner.ImprovementEvaluationRunner`

Run fresh-resource baseline/candidate pairs and stage scoped case evidence.

```python
from agnoclaw import ImprovementEvaluationRunner

ImprovementEvaluationRunner(
    artifact_store: 'ArtifactStore',
    *,
    artifact_scope: 'ArtifactScope',
    evaluator_identity_digest: 'str',
    per_rollout_timeout: 'float' = 120.0,
    max_cases: 'int' = 5000,
    max_case_bytes: 'int' = 262144,
    max_total_case_bytes: 'int' = 16777216,
) -> 'None'
```

## `agnoclaw.learning`

### `LearningMode`

Class · `agnoclaw.learning.LearningMode`

Agno learning modes exposed without leaking Agno enum types.

```python
from agnoclaw import LearningMode

LearningMode(*values)
```

### `LearningPolicy`

Class · `agnoclaw.learning.LearningPolicy`

Static learning intent compiled once with a reusable harness.

```python
from agnoclaw import LearningPolicy

LearningPolicy(
    user_profile: 'LearningStorePolicy | None' = None,
    user_memory: 'LearningStorePolicy | None' = None,
    session_context: 'LearningStorePolicy | None' = None,
    entity_memory: 'LearningStorePolicy | None' = None,
    learned_knowledge: 'LearningStorePolicy | None' = None,
    decision_log: 'LearningStorePolicy | None' = None,
    namespace: 'str | None' = None,
    promotion: 'LearningPromotion' = <LearningPromotion.REVIEWED: 'reviewed'>,
    tenant_required: 'bool' = False,
    consent_required: 'bool' = False,
    retention_days: 'int | None' = None,
    knowledge: 'Any' = None,
) -> None
```

### `LearningProfile`

Class · `agnoclaw.learning.LearningProfile`

Small named constructors for common :class:`LearningPolicy` shapes.

```python
from agnoclaw import LearningProfile

LearningProfile()
```

### `LearningPromotion`

Class · `agnoclaw.learning.LearningPromotion`

Promotion authority for evaluated learning candidates.

```python
from agnoclaw import LearningPromotion

LearningPromotion(*values)
```

### `LearningScope`

Class · `agnoclaw.learning.LearningScope`

Trusted run identity mapped to collision-resistant Agno storage keys.

```python
from agnoclaw import LearningScope

LearningScope(
    tenant_id: 'str | None',
    org_id: 'str | None',
    agent_id: 'str',
    user_id: 'str | None',
    session_id: 'str | None',
    namespace: 'str | None',
    storage_namespace: 'str',
    storage_user_id: 'str | None',
    storage_session_id: 'str | None',
    retention_days: 'int | None',
    consented: 'bool',
) -> None
```

### `LearningStorePolicy`

Class · `agnoclaw.learning.LearningStorePolicy`

Policy for one Agno Learning Store.

```python
from agnoclaw import LearningStorePolicy

LearningStorePolicy(
    mode: 'LearningMode',
    write_path: 'LearningWritePath' = <LearningWritePath.DIRECT: 'direct'>,
    max_updates_per_run: 'int' = 5,
) -> None
```

### `LearningWritePath`

Class · `agnoclaw.learning.LearningWritePath`

Where a store's proposed writes must go.

```python
from agnoclaw import LearningWritePath

LearningWritePath(*values)
```

## `agnoclaw.learning_admin`

### `LearningAdminBackendError`

Class · `agnoclaw.learning_admin.LearningAdminBackendError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningAdminBackendError

LearningAdminBackendError(*, operation: 'str', store: 'LearningDataStore') -> 'None'
```

### `LearningAdminGateway`

Class · `agnoclaw.learning_admin.LearningAdminGateway`

Read/replace/forget exact Agno identity-keyed stores with verification.

```python
from agnoclaw import LearningAdminGateway

LearningAdminGateway(machine: 'Any', *, policy: 'LearningPolicy', scope: 'LearningScope', agent_id: 'str') -> 'None'
```

### `LearningDataRecord`

Class · `agnoclaw.learning_admin.LearningDataRecord`

LearningDataRecord(store: 'LearningDataStore', scope_digest: 'str', present: 'bool', content: 'Any' = None, content_digest: 'str | None' = None, observed_at: 'str' = <factory>, schema_version: 'str' = '1.0')

```python
from agnoclaw import LearningDataRecord

LearningDataRecord(
    store: 'LearningDataStore',
    scope_digest: 'str',
    present: 'bool',
    content: 'Any' = None,
    content_digest: 'str | None' = None,
    observed_at: 'str' = <factory>,
    schema_version: 'str' = '1.0',
) -> None
```

### `LearningDataStore`

Class · `agnoclaw.learning_admin.LearningDataStore`

Enum where members are also (and must be) strings

```python
from agnoclaw import LearningDataStore

LearningDataStore(*values)
```

### `LearningMutationAction`

Class · `agnoclaw.learning_admin.LearningMutationAction`

Enum where members are also (and must be) strings

```python
from agnoclaw import LearningMutationAction

LearningMutationAction(*values)
```

### `LearningMutationReceipt`

Class · `agnoclaw.learning_admin.LearningMutationReceipt`

LearningMutationReceipt(operation_id: 'str', action: 'LearningMutationAction', store: 'LearningDataStore', scope_digest: 'str', existed_before: 'bool', content_digest_before: 'str | None', content_digest_after: 'str | None', backend_delete_confirmed: 'bool | None', verified: 'bool', verification_level: 'LearningVerificationLevel' = <LearningVerificationLevel.POINT_IN_TIME: 'point_in_time'>, completed_at: 'str' = <factory>, schema_version: 'str' = '1.0')

```python
from agnoclaw import LearningMutationReceipt

LearningMutationReceipt(
    operation_id: 'str',
    action: 'LearningMutationAction',
    store: 'LearningDataStore',
    scope_digest: 'str',
    existed_before: 'bool',
    content_digest_before: 'str | None',
    content_digest_after: 'str | None',
    backend_delete_confirmed: 'bool | None',
    verified: 'bool',
    verification_level: 'LearningVerificationLevel' = <LearningVerificationLevel.POINT_IN_TIME: 'point_in_time'>,
    completed_at: 'str' = <factory>,
    schema_version: 'str' = '1.0',
) -> None
```

### `LearningVerificationLevel`

Class · `agnoclaw.learning_admin.LearningVerificationLevel`

Enum where members are also (and must be) strings

```python
from agnoclaw import LearningVerificationLevel

LearningVerificationLevel(*values)
```

## `agnoclaw.learning_candidates`

### `AgnoLearningPromotionAdapter`

Class · `agnoclaw.learning_candidates.AgnoLearningPromotionAdapter`

Narrow Agno adapter for uniquely named, reversibly promoted learnings.

```python
from agnoclaw import AgnoLearningPromotionAdapter

AgnoLearningPromotionAdapter(machine_factory: 'Callable[[LearningCandidate], Any]') -> 'None'
```

### `CandidateAction`

Class · `agnoclaw.learning_candidates.CandidateAction`

Enum where members are also (and must be) strings

```python
from agnoclaw import CandidateAction

CandidateAction(*values)
```

### `CandidateAuthor`

Class · `agnoclaw.learning_candidates.CandidateAuthor`

Enum where members are also (and must be) strings

```python
from agnoclaw import CandidateAuthor

CandidateAuthor(*values)
```

### `CandidateEvaluation`

Class · `agnoclaw.learning_candidates.CandidateEvaluation`

CandidateEvaluation(evaluation_id: 'str', candidate_id: 'str', verdict: 'EvaluationVerdict', evaluator_digest: 'str', evidence_artifact_ids: 'tuple[str, ...]', safety_passed: 'bool', evaluated_by: 'PromotionActor', metrics: 'Any' = <factory>, control_metrics: 'Any' = <factory>, notes: 'str | None' = None, evaluated_at: 'str' = <factory>)

```python
from agnoclaw import CandidateEvaluation

CandidateEvaluation(
    evaluation_id: 'str',
    candidate_id: 'str',
    verdict: 'EvaluationVerdict',
    evaluator_digest: 'str',
    evidence_artifact_ids: 'tuple[str, ...]',
    safety_passed: 'bool',
    evaluated_by: 'PromotionActor',
    metrics: 'Any' = <factory>,
    control_metrics: 'Any' = <factory>,
    notes: 'str | None' = None,
    evaluated_at: 'str' = <factory>,
) -> None
```

### `CandidateEvaluationNotFoundError`

Class · `agnoclaw.learning_candidates.CandidateEvaluationNotFoundError`

Stable error shape for public runtime operations.

```python
from agnoclaw import CandidateEvaluationNotFoundError

CandidateEvaluationNotFoundError(evaluation_id: 'str')
```

### `CandidateReconciliation`

Class · `agnoclaw.learning_candidates.CandidateReconciliation`

CandidateReconciliation(reconciliation_id: 'str', candidate_id: 'str', kind: 'ReconciliationKind', verdict: 'ReconciliationVerdict', reconciler_digest: 'str', evidence_artifact_ids: 'tuple[str, ...]', reconciled_by: 'PromotionActor', target_reference: 'str | None' = None, notes: 'str | None' = None, reconciled_at: 'str' = <factory>)

```python
from agnoclaw import CandidateReconciliation

CandidateReconciliation(
    reconciliation_id: 'str',
    candidate_id: 'str',
    kind: 'ReconciliationKind',
    verdict: 'ReconciliationVerdict',
    reconciler_digest: 'str',
    evidence_artifact_ids: 'tuple[str, ...]',
    reconciled_by: 'PromotionActor',
    target_reference: 'str | None' = None,
    notes: 'str | None' = None,
    reconciled_at: 'str' = <factory>,
) -> None
```

### `CandidateReconciliationNotFoundError`

Class · `agnoclaw.learning_candidates.CandidateReconciliationNotFoundError`

Stable error shape for public runtime operations.

```python
from agnoclaw import CandidateReconciliationNotFoundError

CandidateReconciliationNotFoundError(reconciliation_id: 'str')
```

### `CandidateRecord`

Class · `agnoclaw.learning_candidates.CandidateRecord`

CandidateRecord(candidate: 'LearningCandidate', state: 'CandidateState' = <CandidateState.CAPTURED: 'captured'>, revision: 'int' = 0, latest_evaluation_id: 'str | None' = None, promotion_id: 'str | None' = None, promotion_request_id: 'str | None' = None, promotion_actor: 'PromotionActor | None' = None, promotion_version: 'int' = 0, target_reference: 'str | None' = None, rollback_id: 'str | None' = None, rollback_request_id: 'str | None' = None, rollback_actor: 'PromotionActor | None' = None, latest_reconciliation_id: 'str | None' = None, updated_at: 'str' = <factory>)

```python
from agnoclaw import CandidateRecord

CandidateRecord(
    candidate: 'LearningCandidate',
    state: 'CandidateState' = <CandidateState.CAPTURED: 'captured'>,
    revision: 'int' = 0,
    latest_evaluation_id: 'str | None' = None,
    promotion_id: 'str | None' = None,
    promotion_request_id: 'str | None' = None,
    promotion_actor: 'PromotionActor | None' = None,
    promotion_version: 'int' = 0,
    target_reference: 'str | None' = None,
    rollback_id: 'str | None' = None,
    rollback_request_id: 'str | None' = None,
    rollback_actor: 'PromotionActor | None' = None,
    latest_reconciliation_id: 'str | None' = None,
    updated_at: 'str' = <factory>,
) -> None
```

### `CandidateRisk`

Class · `agnoclaw.learning_candidates.CandidateRisk`

Enum where members are also (and must be) strings

```python
from agnoclaw import CandidateRisk

CandidateRisk(*values)
```

### `CandidateState`

Class · `agnoclaw.learning_candidates.CandidateState`

Enum where members are also (and must be) strings

```python
from agnoclaw import CandidateState

CandidateState(*values)
```

### `EvaluationArchiveCursor`

Class · `agnoclaw.learning_candidates.EvaluationArchiveCursor`

Owner-bound descending keyset cursor for evaluation history.

```python
from agnoclaw import EvaluationArchiveCursor

EvaluationArchiveCursor(evaluated_at: 'str', evaluation_id: 'str', owner_digest: 'str') -> None
```

### `EvaluationArchiveEntry`

Class · `agnoclaw.learning_candidates.EvaluationArchiveEntry`

Content-free operator projection over one immutable candidate evaluation.

```python
from agnoclaw import EvaluationArchiveEntry

EvaluationArchiveEntry(
    evaluation_id: 'str',
    candidate_id: 'str',
    verdict: 'EvaluationVerdict',
    evaluator_digest: 'str',
    evaluated_by: 'PromotionActor',
    safety_passed: 'bool',
    evaluated_at: 'str',
    candidate_state: 'CandidateState',
    target: 'LearningTarget',
    mechanism_version: 'str',
    failure_reason_codes: 'tuple[str, ...]',
    policy_digest: 'str | None',
    hypothesis_digest: 'str | None',
    evaluation_digest: 'str | None',
    runner_digest: 'str | None',
    corpus_manifest_digest: 'str | None',
    evidence_artifact_count: 'int',
) -> None
```

### `EvaluationArchiveLedger`

Class · `agnoclaw.learning_candidates.EvaluationArchiveLedger`

Optional owner-scoped negative-evaluation read model for custom ledgers.

```python
from agnoclaw import EvaluationArchiveLedger

EvaluationArchiveLedger(*args, **kwargs)
```

### `EvaluationArchivePage`

Class · `agnoclaw.learning_candidates.EvaluationArchivePage`

EvaluationArchivePage(items: 'tuple[EvaluationArchiveEntry, ...]', next_cursor: 'EvaluationArchiveCursor | None')

```python
from agnoclaw import EvaluationArchivePage

EvaluationArchivePage(
    items: 'tuple[EvaluationArchiveEntry, ...]',
    next_cursor: 'EvaluationArchiveCursor | None',
) -> None
```

### `EvaluationArchiveQuery`

Class · `agnoclaw.learning_candidates.EvaluationArchiveQuery`

Bounded content-free evaluation archive filters; negative results by default.

```python
from agnoclaw import EvaluationArchiveQuery

EvaluationArchiveQuery(
    verdicts: 'tuple[EvaluationVerdict, ...]' = (<EvaluationVerdict.REJECTED: 'rejected'>, <EvaluationVerdict.INCONCLUSIVE: 'inconclusive'>),
    evaluator_digest: 'str | None' = None,
    reason_code: 'str | None' = None,
    mechanism_version: 'str | None' = None,
    target: 'LearningTarget | None' = None,
    safety_passed: 'bool | None' = None,
    limit: 'int' = 100,
    cursor: 'EvaluationArchiveCursor | None' = None,
) -> None
```

### `EvaluationVerdict`

Class · `agnoclaw.learning_candidates.EvaluationVerdict`

Enum where members are also (and must be) strings

```python
from agnoclaw import EvaluationVerdict

EvaluationVerdict(*values)
```

### `LearningApplication`

Class · `agnoclaw.learning_candidates.LearningApplication`

Content-free attribution that a promoted learning reached one run.

```python
from agnoclaw import LearningApplication

LearningApplication(
    application_id: 'str',
    candidate_id: 'str',
    run_id: 'str',
    target_reference: 'str',
    kind: 'LearningApplicationKind',
    observer_digest: 'str',
    evidence_artifact_ids: 'tuple[str, ...]',
    observed_at: 'str' = <factory>,
) -> None
```

### `LearningApplicationKind`

Class · `agnoclaw.learning_candidates.LearningApplicationKind`

How a promoted learning influenced one run.

```python
from agnoclaw import LearningApplicationKind

LearningApplicationKind(*values)
```

### `LearningApplicationNotFoundError`

Class · `agnoclaw.learning_candidates.LearningApplicationNotFoundError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningApplicationNotFoundError

LearningApplicationNotFoundError(application_id: 'str')
```

### `LearningAttributionConflictError`

Class · `agnoclaw.learning_candidates.LearningAttributionConflictError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningAttributionConflictError

LearningAttributionConflictError(*, record_id: 'str')
```

### `LearningCandidate`

Class · `agnoclaw.learning_candidates.LearningCandidate`

Immutable candidate metadata; content remains in an ArtifactStore.

```python
from agnoclaw import LearningCandidate

LearningCandidate(
    candidate_id: 'str',
    target: 'LearningTarget',
    tenant_id: 'str | None',
    storage_namespace: 'str',
    content_artifact: 'ArtifactReference',
    source_run_ids: 'tuple[str, ...]',
    evidence_artifact_ids: 'tuple[str, ...]',
    confidence: 'float',
    risk: 'CandidateRisk',
    created_by: 'CandidateAuthor',
    mechanism_version: 'str',
    source_user_id: 'str | None' = None,
    expires_at: 'str | None' = None,
    change_hypothesis_artifact_id: 'str | None' = None,
    component_manifest_artifact_id: 'str | None' = None,
    supersedes_candidate_id: 'str | None' = None,
    created_at: 'str' = <factory>,
    schema_version: 'str' = '1.0',
) -> None
```

### `LearningCandidateExport`

Class · `agnoclaw.learning_candidates.LearningCandidateExport`

Portable inspection record; deleted candidates intentionally omit content.

```python
from agnoclaw import LearningCandidateExport

LearningCandidateExport(
    record: 'CandidateRecord',
    content: 'Any',
    evaluations: 'tuple[CandidateEvaluation, ...]',
    reconciliations: 'tuple[CandidateReconciliation, ...]',
    exported_at: 'str' = <factory>,
) -> None
```

### `LearningEffectivenessPolicy`

Class · `agnoclaw.learning_candidates.LearningEffectivenessPolicy`

Conservative thresholds for a non-mutating effectiveness recommendation.

```python
from agnoclaw import LearningEffectivenessPolicy

LearningEffectivenessPolicy(
    minimum_outcomes: 'int' = 5,
    minimum_independent_runs: 'int' = 3,
    review_mean_score: 'float' = 0.0,
    quarantine_mean_score: 'float' = -0.25,
    quarantine_negative_fraction: 'float' = 0.6,
) -> None
```

### `LearningEffectivenessRecommendation`

Class · `agnoclaw.learning_candidates.LearningEffectivenessRecommendation`

Read-only host recommendation; never an automatic state transition.

```python
from agnoclaw import LearningEffectivenessRecommendation

LearningEffectivenessRecommendation(*values)
```

### `LearningEffectivenessSummary`

Class · `agnoclaw.learning_candidates.LearningEffectivenessSummary`

LearningEffectivenessSummary(candidate_id: 'str', total_applications: 'int', applied_applications: 'int', evaluated_outcomes: 'int', independent_runs: 'int', successes: 'int', failures: 'int', corrections: 'int', neutral: 'int', mean_score: 'float | None', recommendation: 'LearningEffectivenessRecommendation')

```python
from agnoclaw import LearningEffectivenessSummary

LearningEffectivenessSummary(
    candidate_id: 'str',
    total_applications: 'int',
    applied_applications: 'int',
    evaluated_outcomes: 'int',
    independent_runs: 'int',
    successes: 'int',
    failures: 'int',
    corrections: 'int',
    neutral: 'int',
    mean_score: 'float | None',
    recommendation: 'LearningEffectivenessRecommendation',
) -> None
```

### `LearningEvent`

Class · `agnoclaw.learning_candidates.LearningEvent`

Canonical content-free event committed with one candidate revision.

```python
from agnoclaw import LearningEvent

LearningEvent(
    event_id: 'str',
    candidate_id: 'str',
    sequence: 'int',
    event_type: 'str',
    occurred_at: 'str',
    tenant_id: 'str | None',
    storage_namespace: 'str',
    payload: 'Any' = <factory>,
) -> None
```

### `LearningGateway`

Class · `agnoclaw.learning_candidates.LearningGateway`

Async candidate capture/evaluation/promotion coordinator.

```python
from agnoclaw import LearningGateway

LearningGateway(
    ledger: 'LearningLedger',
    artifact_store: 'ArtifactStore',
    *,
    promotion_adapter: 'LearningPromotionAdapter | None' = None,
) -> 'None'
```

### `LearningLedger`

Class · `agnoclaw.learning_candidates.LearningLedger`

Base class for protocol classes.

```python
from agnoclaw import LearningLedger

LearningLedger(*args, **kwargs)
```

### `LearningOutboxItem`

Class · `agnoclaw.learning_candidates.LearningOutboxItem`

LearningOutboxItem(outbox_id: 'int', event: 'LearningEvent', lease_owner: 'str', lease_token: 'str', lease_expires_at: 'str')

```python
from agnoclaw import LearningOutboxItem

LearningOutboxItem(
    outbox_id: 'int',
    event: 'LearningEvent',
    lease_owner: 'str',
    lease_token: 'str',
    lease_expires_at: 'str',
) -> None
```

### `LearningOutboxLeaseError`

Class · `agnoclaw.learning_candidates.LearningOutboxLeaseError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningOutboxLeaseError

LearningOutboxLeaseError(outbox_id: 'int')
```

### `LearningOutcome`

Class · `agnoclaw.learning_candidates.LearningOutcome`

Independent, immutable outcome attached to one applied learning.

```python
from agnoclaw import LearningOutcome

LearningOutcome(
    outcome_id: 'str',
    application_id: 'str',
    candidate_id: 'str',
    run_id: 'str',
    kind: 'LearningOutcomeKind',
    score: 'float',
    evaluator_digest: 'str',
    evidence_artifact_ids: 'tuple[str, ...]',
    evaluated_by: 'PromotionActor',
    recorded_at: 'str' = <factory>,
) -> None
```

### `LearningOutcomeKind`

Class · `agnoclaw.learning_candidates.LearningOutcomeKind`

Externally observed result of applying one promoted learning.

```python
from agnoclaw import LearningOutcomeKind

LearningOutcomeKind(*values)
```

### `LearningOutcomeLedger`

Class · `agnoclaw.learning_candidates.LearningOutcomeLedger`

Optional durable attribution extension for custom learning ledgers.

```python
from agnoclaw import LearningOutcomeLedger

LearningOutcomeLedger(*args, **kwargs)
```

### `LearningOutcomeNotFoundError`

Class · `agnoclaw.learning_candidates.LearningOutcomeNotFoundError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningOutcomeNotFoundError

LearningOutcomeNotFoundError(outcome_id: 'str')
```

### `LearningOwner`

Class · `agnoclaw.learning_candidates.LearningOwner`

Exact authorization tuple for a candidate namespace.

```python
from agnoclaw import LearningOwner

LearningOwner(tenant_id: 'str | None', storage_namespace: 'str') -> None
```

### `LearningPromotionAdapter`

Class · `agnoclaw.learning_candidates.LearningPromotionAdapter`

Base class for protocol classes.

```python
from agnoclaw import LearningPromotionAdapter

LearningPromotionAdapter(*args, **kwargs)
```

### `LearningPromotionUnknownError`

Class · `agnoclaw.learning_candidates.LearningPromotionUnknownError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningPromotionUnknownError

LearningPromotionUnknownError(candidate_id: 'str')
```

### `LearningReconciliationWorkerLease`

Class · `agnoclaw.learning_candidates.LearningReconciliationWorkerLease`

One exact-owner maintenance lease with a durable sweep cursor.

```python
from agnoclaw import LearningReconciliationWorkerLease

LearningReconciliationWorkerLease(
    owner_digest: 'str',
    worker_id: 'str',
    lease_token: 'str',
    fence: 'int',
    lease_expires_at: 'str',
    cursor: 'ReconciliationCursor | None' = None,
) -> None
```

### `LearningReconciliationWorkerLeaseError`

Class · `agnoclaw.learning_candidates.LearningReconciliationWorkerLeaseError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningReconciliationWorkerLeaseError

LearningReconciliationWorkerLeaseError() -> 'None'
```

### `LearningRollbackAdapter`

Class · `agnoclaw.learning_candidates.LearningRollbackAdapter`

Base class for protocol classes.

```python
from agnoclaw import LearningRollbackAdapter

LearningRollbackAdapter(*args, **kwargs)
```

### `LearningRollbackUnknownError`

Class · `agnoclaw.learning_candidates.LearningRollbackUnknownError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningRollbackUnknownError

LearningRollbackUnknownError(candidate_id: 'str')
```

### `LearningTarget`

Class · `agnoclaw.learning_candidates.LearningTarget`

Enum where members are also (and must be) strings

```python
from agnoclaw import LearningTarget

LearningTarget(*values)
```

### `PromotionActor`

Class · `agnoclaw.learning_candidates.PromotionActor`

Enum where members are also (and must be) strings

```python
from agnoclaw import PromotionActor

PromotionActor(*values)
```

### `ReconciliationCursor`

Class · `agnoclaw.learning_candidates.ReconciliationCursor`

Owner-bound keyset cursor for restart-safe unknown-effect discovery.

```python
from agnoclaw import ReconciliationCursor

ReconciliationCursor(updated_at: 'str', candidate_id: 'str', owner_digest: 'str') -> None
```

### `ReconciliationCursorScopeError`

Class · `agnoclaw.learning_candidates.ReconciliationCursorScopeError`

Stable error shape for public runtime operations.

```python
from agnoclaw import ReconciliationCursorScopeError

ReconciliationCursorScopeError() -> 'None'
```

### `ReconciliationKind`

Class · `agnoclaw.learning_candidates.ReconciliationKind`

Enum where members are also (and must be) strings

```python
from agnoclaw import ReconciliationKind

ReconciliationKind(*values)
```

### `ReconciliationPage`

Class · `agnoclaw.learning_candidates.ReconciliationPage`

ReconciliationPage(items: 'tuple[ReconciliationRequest, ...]', next_cursor: 'ReconciliationCursor | None' = None)

```python
from agnoclaw import ReconciliationPage

ReconciliationPage(
    items: 'tuple[ReconciliationRequest, ...]',
    next_cursor: 'ReconciliationCursor | None' = None,
) -> None
```

### `ReconciliationRequest`

Class · `agnoclaw.learning_candidates.ReconciliationRequest`

One unknown promotion/rollback that requires observation, never replay.

```python
from agnoclaw import ReconciliationRequest

ReconciliationRequest(record: 'CandidateRecord', kind: 'ReconciliationKind') -> None
```

### `ReconciliationVerdict`

Class · `agnoclaw.learning_candidates.ReconciliationVerdict`

Enum where members are also (and must be) strings

```python
from agnoclaw import ReconciliationVerdict

ReconciliationVerdict(*values)
```

### `SQLiteLearningLedger`

Class · `agnoclaw.learning_candidates.SQLiteLearningLedger`

Transactional local learning candidate authority.

```python
from agnoclaw import SQLiteLearningLedger

SQLiteLearningLedger(path: 'str | Path') -> 'None'
```

## `agnoclaw.learning_postgres`

### `LearningLedgerConnectionLostError`

Class · `agnoclaw.learning_postgres.LearningLedgerConnectionLostError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningLedgerConnectionLostError

LearningLedgerConnectionLostError() -> 'None'
```

### `LearningLedgerDependencyError`

Class · `agnoclaw.learning_postgres.LearningLedgerDependencyError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningLedgerDependencyError

LearningLedgerDependencyError() -> 'None'
```

### `LearningLedgerOverloadedError`

Class · `agnoclaw.learning_postgres.LearningLedgerOverloadedError`

Stable error shape for public runtime operations.

```python
from agnoclaw import LearningLedgerOverloadedError

LearningLedgerOverloadedError(*, retry_after_seconds: 'float') -> 'None'
```

### `PostgresLearningLedger`

Class · `agnoclaw.learning_postgres.PostgresLearningLedger`

Service learning ledger using row locks, CAS, and a bounded sync pool.

```python
from agnoclaw import PostgresLearningLedger

PostgresLearningLedger(
    dsn: 'str',
    *,
    min_pool_size: 'int' = 1,
    max_pool_size: 'int' = 10,
    max_waiting: 'int' = 100,
    pool_timeout_seconds: 'float' = 5.0,
    connect_timeout_seconds: 'float' = 10.0,
    application_name: 'str' = 'agnoclaw-learning',
    fault_injector: 'Callable[[str], None] | None' = None,
) -> 'None'
```

## `agnoclaw.learning_reconciliation`

### `AgnoLearnedKnowledgeReconciliationObserver`

Class · `agnoclaw.learning_reconciliation.AgnoLearnedKnowledgeReconciliationObserver`

Inspect Agno's exact candidate-derived vector key and stage safe evidence.

```python
from agnoclaw import AgnoLearnedKnowledgeReconciliationObserver

AgnoLearnedKnowledgeReconciliationObserver(
    machine_factory: 'Callable[[LearningCandidate], Any]',
    artifact_store: 'ArtifactStore',
    *,
    observer_identity_digest: 'str',
) -> 'None'
```

### `LearningReconciliationCoordinator`

Class · `agnoclaw.learning_reconciliation.LearningReconciliationCoordinator`

Observe one bounded page and CAS-commit only verified results.

```python
from agnoclaw import LearningReconciliationCoordinator

LearningReconciliationCoordinator(
    gateway: 'LearningGateway',
    observer: 'LearningReconciliationObserver',
    *,
    reconciler_digest: 'str',
    reconciled_by: 'PromotionActor' = <PromotionActor.HOST: 'host'>,
    max_concurrency: 'int' = 4,
) -> 'None'
```

### `LearningReconciliationObserver`

Class · `agnoclaw.learning_reconciliation.LearningReconciliationObserver`

Read-only external-state observer supplied and versioned by the host.

```python
from agnoclaw import LearningReconciliationObserver

LearningReconciliationObserver(*args, **kwargs)
```

### `ReconciliationBatchOutcome`

Class · `agnoclaw.learning_reconciliation.ReconciliationBatchOutcome`

ReconciliationBatchOutcome(owner_digest: 'str', items: 'tuple[ReconciliationItemOutcome, ...]', next_cursor: 'ReconciliationCursor | None')

```python
from agnoclaw import ReconciliationBatchOutcome

ReconciliationBatchOutcome(
    owner_digest: 'str',
    items: 'tuple[ReconciliationItemOutcome, ...]',
    next_cursor: 'ReconciliationCursor | None',
) -> None
```

### `ReconciliationItemOutcome`

Class · `agnoclaw.learning_reconciliation.ReconciliationItemOutcome`

ReconciliationItemOutcome(candidate_id: 'str', kind: 'ReconciliationKind', status: 'ReconciliationItemStatus', observed_revision: 'int', resulting_state: 'CandidateState | None' = None, resulting_revision: 'int | None' = None, reconciliation_id: 'str | None' = None, error_code: 'str | None' = None)

```python
from agnoclaw import ReconciliationItemOutcome

ReconciliationItemOutcome(
    candidate_id: 'str',
    kind: 'ReconciliationKind',
    status: 'ReconciliationItemStatus',
    observed_revision: 'int',
    resulting_state: 'CandidateState | None' = None,
    resulting_revision: 'int | None' = None,
    reconciliation_id: 'str | None' = None,
    error_code: 'str | None' = None,
) -> None
```

### `ReconciliationItemStatus`

Class · `agnoclaw.learning_reconciliation.ReconciliationItemStatus`

Enum where members are also (and must be) strings

```python
from agnoclaw import ReconciliationItemStatus

ReconciliationItemStatus(*values)
```

### `ReconciliationObservation`

Class · `agnoclaw.learning_reconciliation.ReconciliationObservation`

One observer result bound to the exact unknown candidate revision.

```python
from agnoclaw import ReconciliationObservation

ReconciliationObservation(
    candidate_id: 'str',
    kind: 'ReconciliationKind',
    expected_revision: 'int',
    candidate_digest: 'str',
    verdict: 'ReconciliationVerdict',
    evidence_artifacts: 'tuple[ArtifactReference, ...]',
    target_reference: 'str | None' = None,
    notes: 'str | None' = None,
) -> None
```

## `agnoclaw.learning_reconciliation_worker`

### `LearningReconciliationLeaseStore`

Class · `agnoclaw.learning_reconciliation_worker.LearningReconciliationLeaseStore`

Optional ledger capability required only by the durable worker.

```python
from agnoclaw import LearningReconciliationLeaseStore

LearningReconciliationLeaseStore(*args, **kwargs)
```

### `LearningReconciliationWorker`

Class · `agnoclaw.learning_reconciliation_worker.LearningReconciliationWorker`

Run bounded reconciliation sweeps under an exact-owner durable lease.

```python
from agnoclaw import LearningReconciliationWorker

LearningReconciliationWorker(
    coordinator: 'LearningReconciliationCoordinator',
    *,
    owner: 'LearningOwner',
    config: 'LearningReconciliationWorkerConfig',
) -> 'None'
```

### `LearningReconciliationWorkerConfig`

Class · `agnoclaw.learning_reconciliation_worker.LearningReconciliationWorkerConfig`

Bounded maintenance-loop configuration.

```python
from agnoclaw import LearningReconciliationWorkerConfig

LearningReconciliationWorkerConfig(
    worker_id: 'str',
    lease_seconds: 'int' = 30,
    poll_interval_seconds: 'float' = 5.0,
    page_limit: 'int' = 100,
    max_concurrency: 'int' = 4,
    heartbeat_interval_seconds: 'float | None' = None,
) -> None
```

### `LearningReconciliationWorkerStats`

Class · `agnoclaw.learning_reconciliation_worker.LearningReconciliationWorkerStats`

Content-free maintenance counters safe for operator output.

```python
from agnoclaw import LearningReconciliationWorkerStats

LearningReconciliationWorkerStats(
    owner_digest: 'str',
    worker_id: 'str',
    claims: 'int' = 0,
    pages: 'int' = 0,
    items: 'int' = 0,
    reconciled: 'int' = 0,
    deferred: 'int' = 0,
    stale: 'int' = 0,
    rejected: 'int' = 0,
    failed: 'int' = 0,
    lease_losses: 'int' = 0,
    store_failures: 'int' = 0,
) -> None
```

## `agnoclaw.migration`

### `inspect_migration_012`

Function · `agnoclaw.migration.inspect_migration_012`

Inspect legacy sources without mutating, locking, copying, or deleting them.

```python
from agnoclaw import inspect_migration_012

inspect_migration_012(
    *,
    learning_sqlite_path: 'str | Path | None' = None,
    schedule_json_path: 'str | Path | None' = None,
    learning_table_names: 'Iterable[str]' = ('agno_learnings', 'agno_memories', 'agnoclaw_memories'),
    scope_mappings: 'Iterable[LegacyLearningScopeMapping]' = (),
    schedule_default_timezone: 'str | None' = None,
    schedule_default_misfire_policy: 'str | ScheduleMisfirePolicy | None' = None,
    old_writer_fence_plan: 'str | None' = None,
    max_learning_rows: 'int' = 100000,
    max_learning_bytes: 'int' = 536870912,
    max_schedule_bytes: 'int' = 16777216,
) -> 'MigrationPreflightReport'
```

### `LearningTableInventory`

Class · `agnoclaw.migration.LearningTableInventory`

LearningTableInventory(table_name: 'str', shape: 'str', row_count: 'int', scanned_rows: 'int', schema_digest: 'str', logical_digest: 'str | None', learning_type_counts: 'tuple[tuple[str, int], ...]' = (), namespace_counts: 'tuple[tuple[str | None, int], ...]' = (), ownership_gap_count: 'int' = 0, collision_count: 'int' = 0)

```python
from agnoclaw import LearningTableInventory

LearningTableInventory(
    table_name: 'str',
    shape: 'str',
    row_count: 'int',
    scanned_rows: 'int',
    schema_digest: 'str',
    logical_digest: 'str | None',
    learning_type_counts: 'tuple[tuple[str, int], ...]' = (),
    namespace_counts: 'tuple[tuple[str | None, int], ...]' = (),
    ownership_gap_count: 'int' = 0,
    collision_count: 'int' = 0,
) -> None
```

### `LegacyLearningScopeMapping`

Class · `agnoclaw.migration.LegacyLearningScopeMapping`

Explicit map-or-quarantine decision for one legacy namespace/type.

```python
from agnoclaw import LegacyLearningScopeMapping

LegacyLearningScopeMapping(
    source_namespace: 'str | None',
    action: 'LegacyScopeAction',
    learning_type: 'str | None' = None,
    target_tenant_id: 'str | None' = None,
    target_namespace: 'str | None' = None,
) -> None
```

### `LegacyScopeAction`

Class · `agnoclaw.migration.LegacyScopeAction`

Enum where members are also (and must be) strings

```python
from agnoclaw import LegacyScopeAction

LegacyScopeAction(*values)
```

### `MigrationFileEvidence`

Class · `agnoclaw.migration.MigrationFileEvidence`

MigrationFileEvidence(role: 'str', path: 'str', size_bytes: 'int', checksum: 'str')

```python
from agnoclaw import MigrationFileEvidence

MigrationFileEvidence(role: 'str', path: 'str', size_bytes: 'int', checksum: 'str') -> None
```

### `MigrationFinding`

Class · `agnoclaw.migration.MigrationFinding`

MigrationFinding(code: 'str', severity: 'MigrationSeverity', source: 'str', safe_message: 'str', resolution: 'str', count: 'int | None' = None)

```python
from agnoclaw import MigrationFinding

MigrationFinding(
    code: 'str',
    severity: 'MigrationSeverity',
    source: 'str',
    safe_message: 'str',
    resolution: 'str',
    count: 'int | None' = None,
) -> None
```

### `MigrationPreflightReport`

Class · `agnoclaw.migration.MigrationPreflightReport`

MigrationPreflightReport(schema_version: 'str', files: 'tuple[MigrationFileEvidence, ...]', learning_tables: 'tuple[LearningTableInventory, ...]', schedule: 'ScheduleInventory | None', scope_mappings: 'tuple[LegacyLearningScopeMapping, ...]', findings: 'tuple[MigrationFinding, ...]', planned_actions: 'tuple[str, ...]', read_only: 'bool' = True, apply_allowed: 'bool' = False)

```python
from agnoclaw import MigrationPreflightReport

MigrationPreflightReport(
    schema_version: 'str',
    files: 'tuple[MigrationFileEvidence, ...]',
    learning_tables: 'tuple[LearningTableInventory, ...]',
    schedule: 'ScheduleInventory | None',
    scope_mappings: 'tuple[LegacyLearningScopeMapping, ...]',
    findings: 'tuple[MigrationFinding, ...]',
    planned_actions: 'tuple[str, ...]',
    read_only: 'bool' = True,
    apply_allowed: 'bool' = False,
) -> None
```

### `MigrationSeverity`

Class · `agnoclaw.migration.MigrationSeverity`

Enum where members are also (and must be) strings

```python
from agnoclaw import MigrationSeverity

MigrationSeverity(*values)
```

### `ScheduleInventory`

Class · `agnoclaw.migration.ScheduleInventory`

ScheduleInventory(format_version: 'str', job_count: 'int', run_count: 'int', logical_digest: 'str', duplicate_job_count: 'int', missing_timezone_count: 'int', missing_misfire_count: 'int', invalid_schedule_count: 'int', unknown_job_run_count: 'int', in_flight_run_count: 'int')

```python
from agnoclaw import ScheduleInventory

ScheduleInventory(
    format_version: 'str',
    job_count: 'int',
    run_count: 'int',
    logical_digest: 'str',
    duplicate_job_count: 'int',
    missing_timezone_count: 'int',
    missing_misfire_count: 'int',
    invalid_schedule_count: 'int',
    unknown_job_run_count: 'int',
    in_flight_run_count: 'int',
) -> None
```

### `ScheduleMisfirePolicy`

Class · `agnoclaw.migration.ScheduleMisfirePolicy`

Enum where members are also (and must be) strings

```python
from agnoclaw import ScheduleMisfirePolicy

ScheduleMisfirePolicy(*values)
```

## `agnoclaw.migration_apply`

### `apply_migration_012`

Function · `agnoclaw.migration_apply.apply_migration_012`

Fence writers, back up every store, and idempotently import local data.

```python
from agnoclaw import apply_migration_012

apply_migration_012(
    plan: 'Migration012Plan',
    *,
    state_dir: 'str | Path',
    confirm_plan_digest: 'str',
    writers_stopped: 'bool',
) -> 'dict[str, Any]'
```

### `create_migration_012_plan`

Function · `agnoclaw.migration_apply.create_migration_012_plan`

Build a deterministic-authority plan without copying or mutating data.

```python
from agnoclaw import create_migration_012_plan

create_migration_012_plan(
    *,
    learning_sqlite_path: 'str | Path | None' = None,
    schedule_json_path: 'str | Path | None' = None,
    target_learning_db: 'str | Path | None' = None,
    target_runtime_db: 'str | Path | None' = None,
    target_tenant_id: 'str | None' = None,
    target_org_id: 'str | None' = None,
    target_agent_id: 'str | None' = None,
    learning_table_names: 'tuple[str, ...]' = ('agno_learnings', 'agno_memories', 'agnoclaw_memories'),
    scope_mappings: 'tuple[LegacyLearningScopeMapping, ...]' = (),
    schedule_default_timezone: 'str | None' = None,
    schedule_default_misfire_policy: 'str | ScheduleMisfirePolicy | None' = None,
    old_writer_fence_plan: 'str | None' = None,
) -> 'Migration012Plan'
```

### `cutover_migration_012`

Function · `agnoclaw.migration_apply.cutover_migration_012`

Record explicit cutover only after independent verification.

```python
from agnoclaw import cutover_migration_012

cutover_migration_012(*, state_dir: 'str | Path', confirm_migration_id: 'str') -> 'dict[str, Any]'
```

### `Migration012Error`

Class · `agnoclaw.migration_apply.Migration012Error`

Safe, structured failure from the 0.12 migration workflow.

```python
from agnoclaw import Migration012Error

Migration012Error(code: 'str', message: 'str', **details: 'Any')
```

### `Migration012Phase`

Class · `agnoclaw.migration_apply.Migration012Phase`

Enum where members are also (and must be) strings

```python
from agnoclaw import Migration012Phase

Migration012Phase(*values)
```

### `Migration012Plan`

Class · `agnoclaw.migration_apply.Migration012Plan`

Content-free, checksum-bound migration intent.

```python
from agnoclaw import Migration012Plan

Migration012Plan(
    migration_id: 'str',
    preflight_digest: 'str',
    planned_at: 'str',
    learning_source: 'str | None',
    schedule_source: 'str | None',
    target_learning_db: 'str | None',
    target_runtime_db: 'str | None',
    target_tenant_id: 'str | None',
    target_org_id: 'str | None',
    target_agent_id: 'str | None',
    learning_tables: 'tuple[str, ...]',
    scope_mappings: 'tuple[LegacyLearningScopeMapping, ...]',
    schedule_timezone: 'str | None',
    schedule_misfire_policy: 'ScheduleMisfirePolicy | None',
    old_writer_fence_plan: 'str | None',
    source_evidence: 'tuple[tuple[str, str, int], ...]',
    rollback_boundary: 'str' = 'before-explicit-schema-contraction-v1',
    schema_version: 'str' = '1.0',
) -> None
```

### `read_migration_012_plan`

Function · `agnoclaw.migration_apply.read_migration_012_plan`

Read and validate one bounded local 0.12 migration plan.

```python
from agnoclaw import read_migration_012_plan

read_migration_012_plan(path: 'str | Path') -> 'Migration012Plan'
```

### `rollback_migration_012`

Function · `agnoclaw.migration_apply.rollback_migration_012`

Restore target preimages and release legacy writer fences.

```python
from agnoclaw import rollback_migration_012

rollback_migration_012(
    *,
    state_dir: 'str | Path',
    confirm_migration_id: 'str',
    writers_stopped: 'bool',
) -> 'dict[str, Any]'
```

### `verify_migration_012`

Function · `agnoclaw.migration_apply.verify_migration_012`

Recompute transformations and compare every imported target identity.

```python
from agnoclaw import verify_migration_012

verify_migration_012(*, state_dir: 'str | Path') -> 'dict[str, Any]'
```

### `write_migration_012_plan`

Function · `agnoclaw.migration_apply.write_migration_012_plan`

Atomically write one validated local 0.12 migration plan.

```python
from agnoclaw import write_migration_012_plan

write_migration_012_plan(path: 'str | Path', plan: 'Migration012Plan') -> 'Path'
```

## `agnoclaw.migration_service`

### `load_postgres_schedule_map`

Function · `agnoclaw.migration_service.load_postgres_schedule_map`

Load a bounded sensitive map and return digest-only plan evidence.

```python
from agnoclaw import load_postgres_schedule_map

load_postgres_schedule_map(path: 'str | Path') -> 'PostgresScheduleMap'
```

### `PostgresMigration012Plan`

Class · `agnoclaw.migration_service.PostgresMigration012Plan`

Digest-bound, content-free intent for a deployment-coordinated migration.

```python
from agnoclaw import PostgresMigration012Plan

PostgresMigration012Plan(
    migration_id: 'str',
    planned_at: 'str',
    source: 'PostgresMigrationDatabaseRef',
    target_learning: 'PostgresMigrationDatabaseRef',
    target_runtime: 'PostgresMigrationDatabaseRef',
    target_tenant_id: 'str',
    target_org_id: 'str | None',
    target_agent_id: 'str',
    scope_mappings: 'tuple[LegacyLearningScopeMapping, ...]',
    schedule_map_summary: 'PostgresScheduleMapSummary',
    backup_receipt: 'PostgresMigrationBackupReceipt',
    writer_fence_plan: 'str',
    endpoint_evidence_digests: 'tuple[tuple[str, str], ...]',
    table_evidence: 'tuple[PostgresMigrationTableEvidence, ...]',
    rollback_boundary: 'str' = 'before-first-legitimate-post-cutover-target-write-v1',
    schema_version: 'str' = '1.0',
    plan_digest: 'str' = '',
) -> None
```

### `PostgresMigrationBackupReceipt`

Class · `agnoclaw.migration_service.PostgresMigrationBackupReceipt`

Reviewed immutable backup evidence referenced by a content-free plan.

```python
from agnoclaw import PostgresMigrationBackupReceipt

PostgresMigrationBackupReceipt(receipt_id: 'str', receipt_digest: 'str', restore_test_id: 'str') -> None
```

### `PostgresMigrationDatabaseRef`

Class · `agnoclaw.migration_service.PostgresMigrationDatabaseRef`

A serializable credential reference, never a resolved PostgreSQL DSN.

```python
from agnoclaw import PostgresMigrationDatabaseRef

PostgresMigrationDatabaseRef(role: 'str', credential_env: 'str', schema: 'str') -> None
```

### `PostgresMigrationTableEvidence`

Class · `agnoclaw.migration_service.PostgresMigrationTableEvidence`

Content-free source or target evidence produced by read-only preflight.

```python
from agnoclaw import PostgresMigrationTableEvidence

PostgresMigrationTableEvidence(
    role: 'str',
    table_name: 'str',
    row_count: 'int',
    schema_digest: 'str',
    logical_digest: 'str | None',
    exists: 'bool' = True,
) -> None
```

### `PostgresScheduleMap`

Class · `agnoclaw.migration_service.PostgresScheduleMap`

PostgresScheduleMap(rules: 'tuple[PostgresScheduleMigrationRule, ...]', map_digest: 'str', source_id_set_digest: 'str')

```python
from agnoclaw import PostgresScheduleMap

PostgresScheduleMap(
    rules: 'tuple[PostgresScheduleMigrationRule, ...]',
    map_digest: 'str',
    source_id_set_digest: 'str',
) -> None
```

### `PostgresScheduleMapSummary`

Class · `agnoclaw.migration_service.PostgresScheduleMapSummary`

Immutable, content-free evidence for a sensitive schedule map.

```python
from agnoclaw import PostgresScheduleMapSummary

PostgresScheduleMapSummary(
    count: 'int',
    map_digest: 'str',
    source_id_set_digest: 'str',
    schema_version: 'str' = '1.1',
) -> None
```

### `PostgresScheduleMigrationRule`

Class · `agnoclaw.migration_service.PostgresScheduleMigrationRule`

Sensitive executable schedule mapping; only its digest enters the plan.

```python
from agnoclaw import PostgresScheduleMigrationRule

PostgresScheduleMigrationRule(
    source_schedule_id: 'str',
    schedule: 'str',
    prompt: 'str',
    tenant_id: 'str',
    user_id: 'str',
    session_id: 'str',
    agent_id: 'str',
    worker_profile: 'str',
    enabled: 'bool',
    isolated: 'bool',
    learning_consent: 'bool',
    timezone: 'str',
    misfire_policy: 'ScheduleMisfirePolicy',
    misfire_grace_seconds: 'int',
    overlap_policy: 'str',
    max_retries: 'int',
    retry_delay_seconds: 'int',
    retry_backoff_multiplier: 'float',
    retry_max_delay_seconds: 'int',
    retry_jitter_seconds: 'int',
    jitter_seconds: 'int',
    concurrency_key: 'str',
) -> None
```

### `read_postgres_migration_012_plan`

Function · `agnoclaw.migration_service.read_postgres_migration_012_plan`

Read and digest-verify one bounded PostgreSQL 0.12 migration plan.

```python
from agnoclaw import read_postgres_migration_012_plan

read_postgres_migration_012_plan(path: 'str | Path') -> 'PostgresMigration012Plan'
```

### `write_postgres_migration_012_plan`

Function · `agnoclaw.migration_service.write_postgres_migration_012_plan`

Atomically write a digest-valid PostgreSQL 0.12 migration plan.

```python
from agnoclaw import write_postgres_migration_012_plan

write_postgres_migration_012_plan(path: 'str | Path', plan: 'PostgresMigration012Plan') -> 'Path'
```

## `agnoclaw.migration_service_apply`

### `apply_postgres_migration_012`

Function · `agnoclaw.migration_service_apply.apply_postgres_migration_012`

Idempotently write reviewed transformations with durable provenance.

```python
from agnoclaw import apply_postgres_migration_012

apply_postgres_migration_012(
    *,
    plan: 'PostgresMigration012Plan',
    schedule_map: 'PostgresScheduleMap',
    confirm_plan_digest: 'str',
    confirm_transform_digest: 'str',
    confirm_backup_receipt_digest: 'str',
    confirm_writer_fence_plan: 'str',
    writers_stopped: 'bool',
    environment: 'dict[str, str] | None' = None,
    statement_timeout_ms: 'int' = 60000,
    lock_timeout_ms: 'int' = 2000,
    max_rows_per_table: 'int' = 10000000,
    read_batch_size: 'int' = 1000,
    write_batch_size: 'int' = 1000,
    max_row_bytes: 'int' = 16777216,
    _checkpoint_observer: '_CheckpointObserver | None' = None,
) -> 'PostgresMigration012LifecycleReceipt'
```

### `cutover_postgres_migration_012`

Function · `agnoclaw.migration_service_apply.cutover_postgres_migration_012`

Verify and record cutover authority without editing deployment configuration.

```python
from agnoclaw import cutover_postgres_migration_012

cutover_postgres_migration_012(
    *,
    plan: 'PostgresMigration012Plan',
    schedule_map: 'PostgresScheduleMap',
    confirm_plan_digest: 'str',
    confirm_transform_digest: 'str',
    confirm_writer_fence_plan: 'str',
    writers_stopped: 'bool',
    cutover_receipt_id: 'str',
    cutover_receipt_digest: 'str',
    environment: 'dict[str, str] | None' = None,
    statement_timeout_ms: 'int' = 60000,
    lock_timeout_ms: 'int' = 2000,
    max_rows_per_table: 'int' = 10000000,
    read_batch_size: 'int' = 1000,
    max_row_bytes: 'int' = 16777216,
    dry_run: 'bool' = False,
) -> 'PostgresMigration012LifecycleReceipt'
```

### `PostgresMigration012LifecycleReceipt`

Class · `agnoclaw.migration_service_apply.PostgresMigration012LifecycleReceipt`

Content-free receipt for one service-migration lifecycle operation.

```python
from agnoclaw import PostgresMigration012LifecycleReceipt

PostgresMigration012LifecycleReceipt(
    operation: 'str',
    migration_id: 'str',
    plan_digest: 'str',
    transform_digest: 'str',
    generated_at: 'str',
    role_phases: 'tuple[tuple[str, str], ...]',
    counts: 'tuple[tuple[str, str, str, int], ...]',
    rollback_available: 'bool',
    receipt_digest: 'str' = '',
    schema_version: 'str' = '1.0',
) -> None
```

### `PostgresMigration012Phase`

Class · `agnoclaw.migration_service_apply.PostgresMigration012Phase`

Enum where members are also (and must be) strings

```python
from agnoclaw import PostgresMigration012Phase

PostgresMigration012Phase(*values)
```

### `rollback_postgres_migration_012`

Function · `agnoclaw.migration_service_apply.rollback_postgres_migration_012`

Remove only exact migration-owned rows and preserve identical preexisting rows.

```python
from agnoclaw import rollback_postgres_migration_012

rollback_postgres_migration_012(
    *,
    plan: 'PostgresMigration012Plan',
    schedule_map: 'PostgresScheduleMap',
    confirm_plan_digest: 'str',
    confirm_transform_digest: 'str',
    confirm_writer_fence_plan: 'str',
    writers_stopped: 'bool',
    confirm_no_post_cutover_target_writes: 'bool' = False,
    environment: 'dict[str, str] | None' = None,
    statement_timeout_ms: 'int' = 60000,
    lock_timeout_ms: 'int' = 2000,
    max_rows_per_table: 'int' = 10000000,
    read_batch_size: 'int' = 1000,
    write_batch_size: 'int' = 1000,
    max_row_bytes: 'int' = 16777216,
    dry_run: 'bool' = False,
    _checkpoint_observer: '_CheckpointObserver | None' = None,
) -> 'PostgresMigration012LifecycleReceipt'
```

### `verify_postgres_migration_012`

Function · `agnoclaw.migration_service_apply.verify_postgres_migration_012`

Independently re-read source, targets, controls, and provenance.

```python
from agnoclaw import verify_postgres_migration_012

verify_postgres_migration_012(
    *,
    plan: 'PostgresMigration012Plan',
    schedule_map: 'PostgresScheduleMap',
    confirm_plan_digest: 'str',
    confirm_transform_digest: 'str',
    confirm_writer_fence_plan: 'str',
    writers_stopped: 'bool',
    environment: 'dict[str, str] | None' = None,
    statement_timeout_ms: 'int' = 60000,
    lock_timeout_ms: 'int' = 2000,
    max_rows_per_table: 'int' = 10000000,
    read_batch_size: 'int' = 1000,
    max_row_bytes: 'int' = 16777216,
    dry_run: 'bool' = False,
) -> 'PostgresMigration012LifecycleReceipt'
```

## `agnoclaw.migration_service_scan`

### `create_postgres_migration_012_plan_from_scan`

Function · `agnoclaw.migration_service_scan.create_postgres_migration_012_plan_from_scan`

Create a plan only when it is bound to a ready, exact scanner result.

```python
from agnoclaw import create_postgres_migration_012_plan_from_scan

create_postgres_migration_012_plan_from_scan(
    *,
    scan: 'PostgresMigration012ScanReport',
    source: 'PostgresMigrationDatabaseRef',
    target_learning: 'PostgresMigrationDatabaseRef',
    target_runtime: 'PostgresMigrationDatabaseRef',
    target_tenant_id: 'str',
    target_agent_id: 'str',
    schedule_map: 'PostgresScheduleMap',
    backup_receipt: 'PostgresMigrationBackupReceipt',
    writer_fence_plan: 'str',
    target_org_id: 'str | None' = None,
    scope_mappings: 'tuple[LegacyLearningScopeMapping, ...]' = (),
) -> 'PostgresMigration012Plan'
```

### `PostgresMigration012ScanReport`

Class · `agnoclaw.migration_service_scan.PostgresMigration012ScanReport`

Content-free evidence and blockers from one read-only database snapshot set.

```python
from agnoclaw import PostgresMigration012ScanReport

PostgresMigration012ScanReport(
    scanned_at: 'str',
    database_reference_digests: 'tuple[tuple[str, str], ...]',
    endpoint_evidence_digests: 'tuple[tuple[str, str], ...]',
    table_evidence: 'tuple[PostgresMigrationTableEvidence, ...]',
    findings: 'tuple[MigrationFinding, ...]',
    source_schedule_id_set_digest: 'str',
    scope_mapping_digest: 'str',
    schema_version: 'str' = '1.0',
) -> None
```

### `scan_postgres_migration_012`

Function · `agnoclaw.migration_service_scan.scan_postgres_migration_012`

Scan source and targets without writes, secrets, or content in the report.

```python
from agnoclaw import scan_postgres_migration_012

scan_postgres_migration_012(
    *,
    source: 'PostgresMigrationDatabaseRef',
    target_learning: 'PostgresMigrationDatabaseRef',
    target_runtime: 'PostgresMigrationDatabaseRef',
    schedule_map: 'PostgresScheduleMap',
    scope_mappings: 'tuple[LegacyLearningScopeMapping, ...]' = (),
    environment: 'dict[str, str] | None' = None,
    statement_timeout_ms: 'int' = 60000,
    lock_timeout_ms: 'int' = 2000,
    max_rows_per_table: 'int' = 10000000,
    batch_size: 'int' = 1000,
    max_row_bytes: 'int' = 16777216,
) -> 'PostgresMigration012ScanReport'
```

## `agnoclaw.migration_service_transform`

### `PostgresMigration012TransformReport`

Class · `agnoclaw.migration_service_transform.PostgresMigration012TransformReport`

Content-free summary of exact source-to-target transformations.

```python
from agnoclaw import PostgresMigration012TransformReport

PostgresMigration012TransformReport(
    migration_id: 'str',
    plan_digest: 'str',
    generated_at: 'str',
    counts: 'tuple[tuple[str, int], ...]',
    category_digests: 'tuple[tuple[str, str], ...]',
    source_table_digests: 'tuple[tuple[str, str], ...]',
    target_identity_digest: 'str',
    transform_digest: 'str' = '',
    schema_version: 'str' = '1.0',
) -> None
```

### `preview_postgres_migration_012_transforms`

Function · `agnoclaw.migration_service_transform.preview_postgres_migration_012_transforms`

Compile every migration row without target writes or content in the report.

```python
from agnoclaw import preview_postgres_migration_012_transforms

preview_postgres_migration_012_transforms(
    *,
    plan: 'PostgresMigration012Plan',
    schedule_map: 'PostgresScheduleMap',
    environment: 'dict[str, str] | None' = None,
    statement_timeout_ms: 'int' = 60000,
    lock_timeout_ms: 'int' = 2000,
    max_rows_per_table: 'int' = 10000000,
    batch_size: 'int' = 1000,
    max_row_bytes: 'int' = 16777216,
) -> 'PostgresMigration012TransformReport'
```

## `agnoclaw.model_factory`

### `AgnoModelFactory`

Class · `agnoclaw.model_factory.AgnoModelFactory`

Create one fresh caller-owned Agno model transport for every harness run.

```python
from agnoclaw import AgnoModelFactory

AgnoModelFactory(
    model_id: 'str',
    implementation_digest: 'str',
    factory: 'Callable[[], Model]',
    provider: 'str | None' = None,
) -> None
```

## `agnoclaw.packs`

### `inspect_pack`

Function · `agnoclaw.packs.inspect_pack`

Parse an `agnoclaw-pack.toml` manifest without executing pack code.

```python
from agnoclaw import inspect_pack

inspect_pack(path: 'str | Path') -> 'PackManifest'
```

### `install_pack`

Function · `agnoclaw.packs.install_pack`

Install a local or git+ pack into the local pack store.

```python
from agnoclaw import install_pack

install_pack(
    source: 'str | Path',
    *,
    root: 'str | Path | None' = None,
    overwrite: 'bool' = False,
) -> 'PackManifest'
```

### `is_pack_trusted`

Function · `agnoclaw.packs.is_pack_trusted`

Return whether a host-owned record trusts this exact installed-pack digest.

```python
from agnoclaw import is_pack_trusted

is_pack_trusted(path_or_name: 'str | Path', *, root: 'str | Path | None' = None) -> 'bool'
```

### `list_installed_packs`

Function · `agnoclaw.packs.list_installed_packs`

List installed pack manifests from the local pack store.

```python
from agnoclaw import list_installed_packs

list_installed_packs(*, root: 'str | Path | None' = None) -> 'list[PackManifest]'
```

### `load_pack`

Function · `agnoclaw.packs.load_pack`

Load a pack manifest and, when trusted, execute registered Python providers.

```python
from agnoclaw import load_pack

load_pack(
    path: 'str | Path',
    *,
    trusted: 'bool' = False,
    trust_root: 'str | Path | None' = None,
) -> 'LoadedPack'
```

### `LoadedPack`

Class · `agnoclaw.packs.LoadedPack`

LoadedPack(manifest: 'PackManifest', trusted: 'bool' = False, capabilities: 'list[Any]' = <factory>, tools: 'list[Any]' = <factory>, skills_dirs: 'list[Path]' = <factory>, pre_run_hooks: 'list[Callable]' = <factory>, post_run_hooks: 'list[Callable]' = <factory>, lifecycle_hooks: 'dict[str, list[Callable]]' = <factory>, context_providers: 'list[Any]' = <factory>, policies: 'list[Any]' = <factory>)

```python
from agnoclaw import LoadedPack

LoadedPack(
    manifest: 'PackManifest',
    trusted: 'bool' = False,
    capabilities: 'list[Any]' = <factory>,
    tools: 'list[Any]' = <factory>,
    skills_dirs: 'list[Path]' = <factory>,
    pre_run_hooks: 'list[Callable]' = <factory>,
    post_run_hooks: 'list[Callable]' = <factory>,
    lifecycle_hooks: 'dict[str, list[Callable]]' = <factory>,
    context_providers: 'list[Any]' = <factory>,
    policies: 'list[Any]' = <factory>,
) -> None
```

### `pack_store_dir`

Function · `agnoclaw.packs.pack_store_dir`

Return the local installed-pack store.

```python
from agnoclaw import pack_store_dir

pack_store_dir(root: 'str | Path | None' = None) -> 'Path'
```

### `PackError`

Class · `agnoclaw.packs.PackError`

Base pack loading error.

```python
from agnoclaw import PackError

PackError(...)
```

### `PackManifest`

Class · `agnoclaw.packs.PackManifest`

PackManifest(name: 'str', version: 'str' = '0.0.0', description: 'str' = '', root: 'Path' = <factory>, provides: 'PackProvides' = <factory>, trust: 'PackTrust' = <factory>)

```python
from agnoclaw import PackManifest

PackManifest(
    name: 'str',
    version: 'str' = '0.0.0',
    description: 'str' = '',
    root: 'Path' = <factory>,
    provides: 'PackProvides' = <factory>,
    trust: 'PackTrust' = <factory>,
) -> None
```

### `PackProvides`

Class · `agnoclaw.packs.PackProvides`

PackProvides(skills: 'list[str]' = <factory>, capabilities: 'list[str]' = <factory>, tools: 'list[str]' = <factory>, hooks: 'list[str]' = <factory>, context_providers: 'list[str]' = <factory>, policies: 'list[str]' = <factory>, commands: 'list[str]' = <factory>)

```python
from agnoclaw import PackProvides

PackProvides(
    skills: 'list[str]' = <factory>,
    capabilities: 'list[str]' = <factory>,
    tools: 'list[str]' = <factory>,
    hooks: 'list[str]' = <factory>,
    context_providers: 'list[str]' = <factory>,
    policies: 'list[str]' = <factory>,
    commands: 'list[str]' = <factory>,
) -> None
```

### `PackTrust`

Class · `agnoclaw.packs.PackTrust`

PackTrust(default: 'str' = 'community', requires_code_execution: 'bool' = False)

```python
from agnoclaw import PackTrust

PackTrust(default: 'str' = 'community', requires_code_execution: 'bool' = False) -> None
```

### `PackTrustError`

Class · `agnoclaw.packs.PackTrustError`

Raised when a pack requires trust before code execution.

```python
from agnoclaw import PackTrustError

PackTrustError(...)
```

### `remove_pack`

Function · `agnoclaw.packs.remove_pack`

Remove an installed pack by name.

```python
from agnoclaw import remove_pack

remove_pack(name: 'str', *, root: 'str | Path | None' = None) -> 'bool'
```

### `trust_pack`

Function · `agnoclaw.packs.trust_pack`

Trust one exact installed-pack digest in a host-owned sidecar store.

```python
from agnoclaw import trust_pack

trust_pack(name: 'str', *, root: 'str | Path | None' = None) -> 'PackManifest'
```

## `agnoclaw.remote`

### `RemoteHarnessClient`

Class · `agnoclaw.remote.RemoteHarnessClient`

Small remote edge for legacy AgentOS calls and versioned agnoclaw runs.

```python
from agnoclaw import RemoteHarnessClient

RemoteHarnessClient(
    base_url: 'str',
    *,
    agent_id: 'str' = 'agnoclaw',
    api_key: 'str | None' = None,
    timeout: 'float' = 60.0,
    client: 'httpx.AsyncClient | None' = None,
) -> 'None'
```

### `RemoteHarnessError`

Class · `agnoclaw.remote.RemoteHarnessError`

A safe, typed error returned by the remote lifecycle service.

```python
from agnoclaw import RemoteHarnessError

RemoteHarnessError(
    code: 'str',
    category: 'str',
    message: 'str',
    retryable: 'bool',
    details: 'dict[str, Any] | None' = None,
) -> None
```

### `RemoteHarnessRun`

Class · `agnoclaw.remote.RemoteHarnessRun`

The existing remote run wrapper, evolved with the HarnessRun lifecycle grammar.

```python
from agnoclaw import RemoteHarnessRun

RemoteHarnessRun(
    result: 'Any' = None,
    run_id: 'str | None' = None,
    session_id: 'str | None' = None,
    _events: 'AsyncIterator[dict[str, Any]] | None' = None,
    _client: 'RemoteHarnessClient | None' = None,
    _harness_id: 'str | None' = None,
    _user_id: 'str | None' = None,
) -> None
```

## `agnoclaw.runtime.agentos`

### `AgentOSClaimKeys`

Class · `agnoclaw.runtime.agentos.AgentOSClaimKeys`

Claim key mapping for AgentOS/JWT payload normalization.

```python
from agnoclaw import AgentOSClaimKeys

AgentOSClaimKeys(
    user_id: tuple[str, ...] = ('user_id', 'sub'),
    session_id: tuple[str, ...] = ('session_id', 'sid'),
    tenant_id: tuple[str, ...] = ('tenant_id', 'tenant'),
    org_id: tuple[str, ...] = ('org_id', 'organization_id', 'org'),
    team_id: tuple[str, ...] = ('team_id', 'team'),
    roles: tuple[str, ...] = ('roles', 'role'),
    scopes: tuple[str, ...] = ('scopes', 'scope', 'permissions'),
    request_id: tuple[str, ...] = ('request_id', 'x_request_id'),
    trace_id: tuple[str, ...] = ('trace_id', 'x_trace_id', 'traceparent'),
) -> None
```

### `AgentOSContextAdapter`

Class · `agnoclaw.runtime.agentos.AgentOSContextAdapter`

Convert AgentOS/JWT claims into the harness `ExecutionContext`.

```python
from agnoclaw import AgentOSContextAdapter

AgentOSContextAdapter(claim_keys: agnoclaw.runtime.agentos.AgentOSClaimKeys | None = None) -> None
```

### `AgentOSHarnessAgent`

Class · `agnoclaw.runtime.agentos.AgentOSHarnessAgent`

AgentOS-compatible facade over an AgentHarness.

```python
from agnoclaw import AgentOSHarnessAgent

AgentOSHarnessAgent(
    harness: Any,
    *,
    id: str | None = None,
    name: str | None = None,
    context_adapter: agnoclaw.runtime.agentos.AgentOSContextAdapter | None = None,
) -> None
```

### `AgentOSPermissionApprover`

Class · `agnoclaw.runtime.agentos.AgentOSPermissionApprover`

Bridge agnoclaw permission requests into AgentOS approval records.

```python
from agnoclaw import AgentOSPermissionApprover

AgentOSPermissionApprover(db: Any, *, agent_id: str, source_name: str = 'agnoclaw') -> None
```

### `as_agentos_agent`

Function · `agnoclaw.runtime.agentos.as_agentos_agent`

Build an AgentOS-compatible agent facade for a harness.

```python
from agnoclaw import as_agentos_agent

as_agentos_agent(
    harness: Any,
    *,
    agent_id: str | None = None,
    name: str | None = None,
    context_adapter: agnoclaw.runtime.agentos.AgentOSContextAdapter | None = None,
) -> agnoclaw.runtime.agentos.AgentOSHarnessAgent
```

### `create_agentos_app`

Function · `agnoclaw.runtime.agentos.create_agentos_app`

Create a FastAPI app backed by Agno AgentOS and agnoclaw harness adapters.

```python
from agnoclaw import create_agentos_app

create_agentos_app(
    harnesses: collections.abc.Sequence[typing.Any],
    *,
    include_agnoclaw_admin: bool = False,
    include_agnoclaw_lifecycle: bool = True,
    child_templates: collections.abc.Mapping[str, collections.abc.Mapping[str, typing.Any]] | None = None,
    enable_mcp_server: bool = False,
    scheduler: bool = False,
    approvals: bool = False,
    **kwargs: Any,
) -> Any
```

## `agnoclaw.runtime.artifacts`

### `ArtifactCorruptError`

Class · `agnoclaw.runtime.artifacts.ArtifactCorruptError`

Stable error shape for public runtime operations.

```python
from agnoclaw import ArtifactCorruptError

ArtifactCorruptError(artifact_id: 'str', *, reason: 'str')
```

### `ArtifactReference`

Class · `agnoclaw.runtime.artifacts.ArtifactReference`

Immutable staged-byte descriptor committed into the runtime ledger.

```python
from agnoclaw import ArtifactReference

ArtifactReference(
    artifact_id: 'str',
    scope: 'ArtifactScope',
    purpose: 'str',
    media_type: 'str',
    encoding: 'str',
    checksum: 'str',
    stored_checksum: 'str',
    size_bytes: 'int',
    stored_size_bytes: 'int',
    storage_key: 'str',
    protection: 'ArtifactProtection | None' = None,
    metadata: 'Any' = <factory>,
    staged_at: 'str' = <factory>,
    schema_version: 'str' = '1.0',
) -> None
```

### `ArtifactScope`

Class · `agnoclaw.runtime.artifacts.ArtifactScope`

Exact ledger ownership namespace for one artifact.

```python
from agnoclaw import ArtifactScope

ArtifactScope(run_id: 'str', tenant_id: 'str | None' = None, user_id: 'str | None' = None) -> None
```

### `ArtifactStore`

Class · `agnoclaw.runtime.artifacts.ArtifactStore`

One async-first external-byte boundary; ledger references remain authoritative.

```python
from agnoclaw import ArtifactStore

ArtifactStore(*args, **kwargs)
```

### `LocalArtifactStore`

Class · `agnoclaw.runtime.artifacts.LocalArtifactStore`

Atomic local-filesystem content store for the single-node durable profile.

```python
from agnoclaw import LocalArtifactStore

LocalArtifactStore(
    root: 'str | Path',
    *,
    max_artifact_bytes: 'int' = 16777216,
    max_stored_bytes: 'int | None' = None,
    max_page_bytes: 'int' = 1048576,
    key_provider: 'KeyProvider | None' = None,
) -> 'None'
```

## `agnoclaw.runtime.child_ingress`

### `DeclaredChildTemplate`

Class · `agnoclaw.runtime.child_ingress.DeclaredChildTemplate`

One host-owned delegation policy usable as a model tool or remote route.

```python
from agnoclaw import DeclaredChildTemplate

DeclaredChildTemplate(
    name: 'str',
    child_harness: 'Any',
    purpose_code: 'str',
    description: 'str' = 'Delegate one bounded task to a declared child agent.',
    version: 'str' = '1.0.0',
    budget: 'ChildRunBudget' = <factory>,
    capability_allowlist: 'tuple[str, ...]' = (),
    join_policy: 'ChildJoinPolicy' = <ChildJoinPolicy.ALL_SUCCESS: 'all_success'>,
    learning_allowed: 'bool' = False,
    result_schema: 'Any' = None,
    persist_output: 'bool' = False,
    required_scopes: 'tuple[str, ...]' = (),
    max_message_chars: 'int' = 60000,
    max_inline_result_chars: 'int' = 8000,
) -> None
```

## `agnoclaw.runtime.child_results`

### `ChildArtifactHandoff`

Class · `agnoclaw.runtime.child_results.ChildArtifactHandoff`

Content-minimized reference a parent may use to retrieve child evidence.

```python
from agnoclaw import ChildArtifactHandoff

ChildArtifactHandoff(
    artifact_id: 'str',
    purpose: 'str',
    media_type: 'str',
    encoding: 'str',
    checksum: 'str',
    size_bytes: 'int',
) -> None
```

### `ChildResultSet`

Class · `agnoclaw.runtime.child_results.ChildResultSet`

Bounded direct-child outcomes with explicit partial-failure semantics.

```python
from agnoclaw import ChildResultSet

ChildResultSet(
    parent_run_id: 'str',
    outcomes: 'tuple[ChildRunOutcome, ...]' = <factory>,
    schema_version: 'str' = '1.0',
) -> None
```

### `ChildRunOutcome`

Class · `agnoclaw.runtime.child_results.ChildRunOutcome`

One direct child's typed state, terminal projection, and artifact handoff.

```python
from agnoclaw import ChildRunOutcome

ChildRunOutcome(
    child_run_id: 'str',
    delegation_id: 'str',
    purpose_code: 'str',
    state: 'RunState',
    result: 'Any' = None,
    safe_error: 'Any' = None,
    artifacts: 'tuple[ChildArtifactHandoff, ...]' = (),
    result_artifact: 'ChildArtifactHandoff | None' = None,
    parent_step_id: 'str | None' = None,
    parent_tool_call_id: 'str | None' = None,
    schema_version: 'str' = '1.0',
) -> None
```

## `agnoclaw.runtime.children`

### `ChildCancellationPolicy`

Class · `agnoclaw.runtime.children.ChildCancellationPolicy`

Cancellation relationship supported by the first child contract.

```python
from agnoclaw import ChildCancellationPolicy

ChildCancellationPolicy(*values)
```

### `ChildJoinPolicy`

Class · `agnoclaw.runtime.children.ChildJoinPolicy`

How a parent interprets one or more terminal child outcomes.

```python
from agnoclaw import ChildJoinPolicy

ChildJoinPolicy(*values)
```

### `ChildRunBudget`

Class · `agnoclaw.runtime.children.ChildRunBudget`

Finite tree and resource grant inherited monotonically by descendants.

```python
from agnoclaw import ChildRunBudget

ChildRunBudget(
    max_depth: 'int' = 4,
    max_fanout: 'int' = 8,
    timeout_seconds: 'int' = 600,
    max_tokens: 'int' = 100000,
    max_cost_microusd: 'int' = 5000000,
) -> None
```

### `ChildRunContractError`

Class · `agnoclaw.runtime.children.ChildRunContractError`

A child declaration tried to violate lineage or bounded authority.

```python
from agnoclaw import ChildRunContractError

ChildRunContractError(*, code: 'str', message: 'str', details: 'dict[str, Any] | None' = None) -> 'None'
```

### `ChildRunSpec`

Class · `agnoclaw.runtime.children.ChildRunSpec`

Immutable, owner-inherited declaration for one logical child run.

```python
from agnoclaw import ChildRunSpec

ChildRunSpec(
    child_run_id: 'str',
    parent_run_id: 'str',
    root_run_id: 'str',
    depth: 'int',
    delegation_id: 'str',
    purpose_code: 'str',
    budget: 'ChildRunBudget' = <factory>,
    capability_allowlist: 'tuple[str, ...]' = (),
    join_policy: 'ChildJoinPolicy' = <ChildJoinPolicy.ALL_SUCCESS: 'all_success'>,
    cancellation_policy: 'ChildCancellationPolicy' = <ChildCancellationPolicy.PROPAGATE: 'propagate'>,
    learning_allowed: 'bool' = False,
    result_schema: 'Any' = None,
    parent_step_id: 'str | None' = None,
    parent_tool_call_id: 'str | None' = None,
    schema_version: 'str' = '1.1',
) -> None
```

## `agnoclaw.runtime.concurrency`

### `RuntimeClosePolicy`

Class · `agnoclaw.runtime.concurrency.RuntimeClosePolicy`

Ownership decision for live lifecycle runs during harness shutdown.

```python
from agnoclaw import RuntimeClosePolicy

RuntimeClosePolicy(*values)
```

## `agnoclaw.runtime.context`

### `ExecutionContext`

Class · `agnoclaw.runtime.context.ExecutionContext`

Immutable run context passed across runtime boundaries.

```python
from agnoclaw import ExecutionContext

ExecutionContext(
    user_id: 'str | None',
    session_id: 'str | None',
    workspace_id: 'str | None',
    tenant_id: 'str | None' = None,
    org_id: 'str | None' = None,
    team_id: 'str | None' = None,
    roles: 'tuple[str, ...]' = (),
    scopes: 'tuple[str, ...]' = (),
    request_id: 'str | None' = None,
    trace_id: 'str | None' = None,
    trusted_permission_tools: 'tuple[str, ...]' = (),
    trusted_permission_categories: 'tuple[str, ...]' = (),
    metadata: 'dict[str, Any]' = <factory>,
    identity_source: 'IdentitySource' = <IdentitySource.TRUSTED_HOST: 'trusted_host'>,
    admission: 'AdmissionEnvelope | None' = None,
) -> None
```

## `agnoclaw.runtime.dead_letters`

### `DeadLetterAdminAuthorizationError`

Class · `agnoclaw.runtime.dead_letters.DeadLetterAdminAuthorizationError`

Stable error shape for public runtime operations.

```python
from agnoclaw import DeadLetterAdminAuthorizationError

DeadLetterAdminAuthorizationError(*, scope: 'str') -> 'None'
```

### `DeadLetterAdminCursorError`

Class · `agnoclaw.runtime.dead_letters.DeadLetterAdminCursorError`

Stable error shape for public runtime operations.

```python
from agnoclaw import DeadLetterAdminCursorError

DeadLetterAdminCursorError() -> 'None'
```

### `DeadLetterAuditPage`

Class · `agnoclaw.runtime.dead_letters.DeadLetterAuditPage`

DeadLetterAuditPage(items: 'tuple[DeadLetterAuditRecord, ...]', next_cursor: 'str | None')

```python
from agnoclaw import DeadLetterAuditPage

DeadLetterAuditPage(items: 'tuple[DeadLetterAuditRecord, ...]', next_cursor: 'str | None') -> None
```

### `DeadLetterPage`

Class · `agnoclaw.runtime.dead_letters.DeadLetterPage`

DeadLetterPage(items: 'tuple[DeadLetterItem, ...]', audit: 'DeadLetterAuditRecord', next_cursor: 'str | None')

```python
from agnoclaw import DeadLetterPage

DeadLetterPage(
    items: 'tuple[DeadLetterItem, ...]',
    audit: 'DeadLetterAuditRecord',
    next_cursor: 'str | None',
) -> None
```

### `RuntimeDeadLetterAdmin`

Class · `agnoclaw.runtime.dead_letters.RuntimeDeadLetterAdmin`

Small host API for scoped inspection, replay, and immutable audit history.

```python
from agnoclaw import RuntimeDeadLetterAdmin

RuntimeDeadLetterAdmin(store: 'RuntimeStore') -> 'None'
```

## `agnoclaw.runtime.errors`

### `AgnoAuthError`

Class · `agnoclaw.runtime.errors.AgnoAuthError`

Raised when model/provider authentication is missing or invalid.

```python
from agnoclaw import AgnoAuthError

AgnoAuthError(message: 'str', *, details: 'dict[str, Any] | None' = None)
```

### `AgnoCapabilityError`

Class · `agnoclaw.runtime.errors.AgnoCapabilityError`

Raised when code requests an unavailable version-sensitive capability.

```python
from agnoclaw import AgnoCapabilityError

AgnoCapabilityError(*, feature: 'str', version: 'str', reason: 'str')
```

### `AgnoConfigError`

Class · `agnoclaw.runtime.errors.AgnoConfigError`

Raised when model/provider configuration is invalid.

```python
from agnoclaw import AgnoConfigError

AgnoConfigError(message: 'str', *, details: 'dict[str, Any] | None' = None)
```

### `AgnoVersionError`

Class · `agnoclaw.runtime.errors.AgnoVersionError`

Raised for an unrecognized or uncertified Agno runtime version.

```python
from agnoclaw import AgnoVersionError

AgnoVersionError(*, version: 'str', reason: 'str')
```

### `HarnessError`

Class · `agnoclaw.runtime.errors.HarnessError`

Stable error shape for public runtime operations.

```python
from agnoclaw import HarnessError

HarnessError(
    code: 'str',
    category: 'str',
    message: 'str',
    retryable: 'bool',
    details: 'dict[str, Any] | None' = None,
) -> None
```

### `ModelProviderDependencyError`

Class · `agnoclaw.runtime.errors.ModelProviderDependencyError`

Raised before Agno imports a model provider SDK that is not installed.

```python
from agnoclaw import ModelProviderDependencyError

ModelProviderDependencyError(*, provider: 'str', package: 'str', install_extra: 'str')
```

## `agnoclaw.runtime.events`

### `EventSinkMode`

Class · `agnoclaw.runtime.events.EventSinkMode`

Enum where members are also (and must be) strings

```python
from agnoclaw import EventSinkMode

EventSinkMode(*values)
```

### `InMemoryEventSink`

Class · `agnoclaw.runtime.events.InMemoryEventSink`

Useful for testing and local instrumentation.

```python
from agnoclaw import InMemoryEventSink

InMemoryEventSink() -> 'None'
```

## `agnoclaw.runtime.gateway`

### `OperationGateway`

Class · `agnoclaw.runtime.gateway.OperationGateway`

Persist intent, fence dispatch, and persist one canonical settlement.

```python
from agnoclaw import OperationGateway

OperationGateway(
    store: 'RuntimeStore',
    *,
    worker_id: 'str',
    result_reference_factory: 'ResultReferenceFactory | None' = None,
    result_loader: 'ResultLoader | None' = None,
    artifact_store: 'ArtifactStore | None' = None,
    artifact_purpose: 'str' = 'operation_result',
    result_serializer: 'ResultSerializer | None' = None,
    result_cache_size: 'int' = 128,
) -> 'None'
```

## `agnoclaw.runtime.guardrails`

### `GuardrailViolation`

Class · `agnoclaw.runtime.guardrails.GuardrailViolation`

A single guardrail violation.

```python
from agnoclaw import GuardrailViolation

GuardrailViolation(code: 'str', message: 'str', details: 'dict[str, Any]' = <factory>) -> None
```

### `RuntimeGuardrails`

Class · `agnoclaw.runtime.guardrails.RuntimeGuardrails`

Path and network guardrail evaluator for tool calls.

```python
from agnoclaw import RuntimeGuardrails

RuntimeGuardrails(
    *,
    workspace_dir: 'str | Path',
    enabled: 'bool' = True,
    path_enabled: 'bool' = True,
    path_allowed_roots: 'Iterable[str] | None' = None,
    path_blocked_roots: 'Iterable[str] | None' = None,
    network_enabled: 'bool' = True,
    network_enforce_https: 'bool' = True,
    network_allowed_hosts: 'Iterable[str] | None' = None,
    network_blocked_hosts: 'Iterable[str] | None' = None,
    network_block_private_hosts: 'bool' = True,
    network_block_in_bash: 'bool' = True,
) -> 'None'
```

## `agnoclaw.runtime.hooks`

### `LifecycleHook`

Class · `agnoclaw.runtime.hooks.LifecycleHook`

Generic lifecycle hook protocol.

```python
from agnoclaw import LifecycleHook

LifecycleHook(*args, **kwargs)
```

### `LifecycleHookRequest`

Class · `agnoclaw.runtime.hooks.LifecycleHookRequest`

Generic lifecycle checkpoint payload for pack and harness hooks.

```python
from agnoclaw import LifecycleHookRequest

LifecycleHookRequest(event_type: 'str', run_id: 'str | None' = None, metadata: 'dict[str, Any]' = <factory>) -> None
```

### `PlanExitSignal`

Class · `agnoclaw.runtime.hooks.PlanExitSignal`

Structured signal that a plan is ready for review.

```python
from agnoclaw import PlanExitSignal

PlanExitSignal(
    signal_id: 'str',
    summary: 'str',
    plan_path: 'str | None' = None,
    ready_for_approval: 'bool' = True,
    metadata: 'dict[str, Any]' = <factory>,
) -> None
```

### `PlanQuestionSignal`

Class · `agnoclaw.runtime.hooks.PlanQuestionSignal`

Structured request for user input during planning.

```python
from agnoclaw import PlanQuestionSignal

PlanQuestionSignal(
    signal_id: 'str',
    question: 'str',
    options: 'list[str]' = <factory>,
    allow_freeform: 'bool' = True,
    metadata: 'dict[str, Any]' = <factory>,
) -> None
```

## `agnoclaw.runtime.inspection`

### `RunInspection`

Class · `agnoclaw.runtime.inspection.RunInspection`

Bounded support/recovery report with no arbitrary runtime content.

```python
from agnoclaw import RunInspection

RunInspection(
    run_id_hash: 'str',
    state: 'str',
    revision: 'int',
    created_at: 'str',
    updated_at: 'str',
    child_depth: 'int',
    parent_run_id_hash: 'str | None',
    pending_request_id_hash: 'str | None',
    event_count_inspected: 'int',
    events_at_limit: 'bool',
    last_event_sequence: 'int | None',
    last_event_type: 'str | None',
    operation_count_inspected: 'int',
    operations_at_limit: 'bool',
    operation_state_counts: 'tuple[tuple[str, int], ...]',
    operations: 'tuple[InspectedOperation, ...]',
    approval_count_inspected: 'int',
    approvals_at_limit: 'bool',
    pending_approval_count: 'int',
    child_count_inspected: 'int',
    children_at_limit: 'bool',
    child_state_counts: 'tuple[tuple[str, int], ...]',
    artifact_count_inspected: 'int',
    artifacts_at_limit: 'bool',
    artifact_bytes_inspected: 'int',
    terminal_result_available: 'bool',
    terminal_error_available: 'bool',
    recommendation: 'RunRecoveryRecommendation',
    schema_version: 'str' = '1.0',
) -> None
```

### `RunInspectionAuthorizationError`

Class · `agnoclaw.runtime.inspection.RunInspectionAuthorizationError`

Stable error shape for public runtime operations.

```python
from agnoclaw import RunInspectionAuthorizationError

RunInspectionAuthorizationError() -> 'None'
```

### `RunRecoveryRecommendation`

Class · `agnoclaw.runtime.inspection.RunRecoveryRecommendation`

Content-free operator action suggested by durable state only.

```python
from agnoclaw import RunRecoveryRecommendation

RunRecoveryRecommendation(*values)
```

### `RuntimeRunInspector`

Class · `agnoclaw.runtime.inspection.RuntimeRunInspector`

Build content-free recovery reports after exact owner/scope authorization.

```python
from agnoclaw import RuntimeRunInspector

RuntimeRunInspector(*, store: 'RuntimeStore', policy: 'RuntimeTelemetryPolicy') -> 'None'
```

## `agnoclaw.runtime.leases`

### `RunLeaseClaim`

Class · `agnoclaw.runtime.leases.RunLeaseClaim`

RunLeaseClaim(run: 'RuntimeLease', session: 'RuntimeLease')

```python
from agnoclaw import RunLeaseClaim

RunLeaseClaim(run: 'RuntimeLease', session: 'RuntimeLease') -> None
```

### `RuntimeLease`

Class · `agnoclaw.runtime.leases.RuntimeLease`

RuntimeLease(lease_key: 'str', kind: 'LeaseKind', run_id: 'str', worker_id: 'str', claim_id: 'str', lease_token: 'str', fence_token: 'int', acquired_at: 'str', expires_at: 'str', schema_version: 'str' = '1.0')

```python
from agnoclaw import RuntimeLease

RuntimeLease(
    lease_key: 'str',
    kind: 'LeaseKind',
    run_id: 'str',
    worker_id: 'str',
    claim_id: 'str',
    lease_token: 'str',
    fence_token: 'int',
    acquired_at: 'str',
    expires_at: 'str',
    schema_version: 'str' = '1.0',
) -> None
```

### `RuntimeLeaseLostError`

Class · `agnoclaw.runtime.leases.RuntimeLeaseLostError`

Stable error shape for public runtime operations.

```python
from agnoclaw import RuntimeLeaseLostError

RuntimeLeaseLostError(*, run_id: 'str', kind: 'LeaseKind')
```

### `RuntimeLeaseUnavailableError`

Class · `agnoclaw.runtime.leases.RuntimeLeaseUnavailableError`

Stable error shape for public runtime operations.

```python
from agnoclaw import RuntimeLeaseUnavailableError

RuntimeLeaseUnavailableError(*, run_id: 'str', kind: 'LeaseKind', retry_after_seconds: 'float')
```

## `agnoclaw.runtime.lifecycle`

### `RunSnapshot`

Class · `agnoclaw.runtime.lifecycle.RunSnapshot`

Authorized lifecycle projection for one logical run.

```python
from agnoclaw import RunSnapshot

RunSnapshot(
    run_id: 'str',
    state: 'RunState' = <RunState.CREATED: 'created'>,
    revision: 'int' = 0,
    schema_version: 'str' = '1.0',
    tenant_id: 'str | None' = None,
    user_id: 'str | None' = None,
    session_id: 'str | None' = None,
    parent_run_id: 'str | None' = None,
    root_run_id: 'str | None' = None,
    child_depth: 'int' = 0,
    created_at: 'str' = <factory>,
    updated_at: 'str' = <factory>,
    steering_open: 'bool' = True,
    pending_request_id: 'str | None' = None,
    last_transition_id: 'str | None' = None,
    last_reason_code: 'str | None' = None,
    metadata: 'Any' = <factory>,
) -> None
```

### `RunState`

Class · `agnoclaw.runtime.lifecycle.RunState`

Enum where members are also (and must be) strings

```python
from agnoclaw import RunState

RunState(*values)
```

## `agnoclaw.runtime.lifecycle_protocol`

### `LifecycleProtocolError`

Class · `agnoclaw.runtime.lifecycle_protocol.LifecycleProtocolError`

The peer returned or supplied an invalid lifecycle protocol envelope.

```python
from agnoclaw import LifecycleProtocolError

LifecycleProtocolError(message: 'str' = 'The lifecycle protocol envelope is invalid.') -> 'None'
```

## `agnoclaw.runtime.operations`

### `EffectClass`

Class · `agnoclaw.runtime.operations.EffectClass`

Externally observable replay semantics, never inferred from a tool name.

```python
from agnoclaw import EffectClass

EffectClass(*values)
```

### `OperationReconciliationVerdict`

Class · `agnoclaw.runtime.operations.OperationReconciliationVerdict`

Independently observed outcome for an ambiguous external dispatch.

```python
from agnoclaw import OperationReconciliationVerdict

OperationReconciliationVerdict(*values)
```

## `agnoclaw.runtime.outbox`

### `RuntimeEventBatchExporter`

Class · `agnoclaw.runtime.outbox.RuntimeEventBatchExporter`

Async destination; consumers deduplicate with ``RuntimeEvent.event_id``.

```python
from agnoclaw import RuntimeEventBatchExporter

RuntimeEventBatchExporter(*args, **kwargs)
```

### `RuntimeOutboxBatchResult`

Class · `agnoclaw.runtime.outbox.RuntimeOutboxBatchResult`

RuntimeOutboxBatchResult(leased: 'int', delivered: 'int', deferred: 'int', dead_lettered: 'int' = 0, first_outbox_id: 'int | None' = None, last_outbox_id: 'int | None' = None, failure_code: 'str | None' = None)

```python
from agnoclaw import RuntimeOutboxBatchResult

RuntimeOutboxBatchResult(
    leased: 'int',
    delivered: 'int',
    deferred: 'int',
    dead_lettered: 'int' = 0,
    first_outbox_id: 'int | None' = None,
    last_outbox_id: 'int | None' = None,
    failure_code: 'str | None' = None,
) -> None
```

### `RuntimeOutboxConfig`

Class · `agnoclaw.runtime.outbox.RuntimeOutboxConfig`

RuntimeOutboxConfig(owner: 'str', batch_size: 'int' = 50, lease_seconds: 'int' = 60, delivery_timeout_seconds: 'float' = 30, retry_base_seconds: 'float' = 1, retry_max_seconds: 'float' = 300, max_attempts: 'int' = 20, idle_poll_seconds: 'float' = 1)

```python
from agnoclaw import RuntimeOutboxConfig

RuntimeOutboxConfig(
    owner: 'str',
    batch_size: 'int' = 50,
    lease_seconds: 'int' = 60,
    delivery_timeout_seconds: 'float' = 30,
    retry_base_seconds: 'float' = 1,
    retry_max_seconds: 'float' = 300,
    max_attempts: 'int' = 20,
    idle_poll_seconds: 'float' = 1,
) -> None
```

### `RuntimeOutboxWorker`

Class · `agnoclaw.runtime.outbox.RuntimeOutboxWorker`

Small restart-safe pump over the RuntimeStore transactional outbox.

```python
from agnoclaw import RuntimeOutboxWorker

RuntimeOutboxWorker(
    *,
    store: 'RuntimeStore',
    exporter: 'RuntimeEventBatchExporter',
    config: 'RuntimeOutboxConfig',
) -> 'None'
```

## `agnoclaw.runtime.output_segments`

### `RunOutputSegment`

Class · `agnoclaw.runtime.output_segments.RunOutputSegment`

One ordered provider-text segment reconstructed from authorized artifacts.

```python
from agnoclaw import RunOutputSegment

RunOutputSegment(
    run_id: 'str',
    segment_sequence: 'int',
    event_sequence: 'int',
    cursor: 'str',
    content: 'str',
    delta_count: 'int',
    artifact_id: 'str',
    occurred_at: 'str',
    schema_version: 'str' = '1.0',
) -> None
```

### `RunOutputSegmentError`

Class · `agnoclaw.runtime.output_segments.RunOutputSegmentError`

A persisted segment failed its typed reference/content contract.

```python
from agnoclaw import RunOutputSegmentError

RunOutputSegmentError(*, run_id: 'str', reason: 'str') -> 'None'
```

## `agnoclaw.runtime.permissions`

### `ElevatedCommandRequest`

Class · `agnoclaw.runtime.permissions.ElevatedCommandRequest`

Host-elevated command request that must pass approval and policy gates.

```python
from agnoclaw import ElevatedCommandRequest

ElevatedCommandRequest(
    run_id: 'str',
    command: 'str',
    reason: 'str',
    working_dir: 'str | None' = None,
    timeout_seconds: 'int | None' = None,
    metadata: 'dict[str, Any]' = <factory>,
) -> None
```

### `ElevatedCommandResult`

Class · `agnoclaw.runtime.permissions.ElevatedCommandResult`

Normalized result from a host-elevated command execution.

```python
from agnoclaw import ElevatedCommandResult

ElevatedCommandResult(
    run_id: 'str',
    command: 'str',
    stdout: 'str',
    stderr: 'str',
    exit_code: 'int',
    duration_ms: 'int | None' = None,
    working_dir: 'str | None' = None,
    metadata: 'dict[str, Any]' = <factory>,
) -> None
```

### `ElevatedSessionMode`

Class · `agnoclaw.runtime.permissions.ElevatedSessionMode`

Session-wide elevated execution modes for interactive CLI sessions.

```python
from agnoclaw import ElevatedSessionMode

ElevatedSessionMode(*values)
```

### `InteractivePermissionApprover`

Class · `agnoclaw.runtime.permissions.InteractivePermissionApprover`

Interactive terminal approver for runtime permission requests.

```python
from agnoclaw import InteractivePermissionApprover

InteractivePermissionApprover(
    *,
    input_fn=<built-in function input>,
    output_fn=<built-in function print>,
    default: 'bool' = False,
) -> 'None'
```

### `PermissionController`

Class · `agnoclaw.runtime.permissions.PermissionController`

Runtime permission-mode evaluator for tool calls.

```python
from agnoclaw import PermissionController

PermissionController(
    *,
    mode: 'str | PermissionMode' = <PermissionMode.BYPASS: 'bypass'>,
    approver: 'PermissionApprover | None' = None,
    require_approver: 'bool' = False,
    preapproved_tools: 'tuple[str, ...]' = (),
    preapproved_categories: 'tuple[str, ...]' = (),
) -> 'None'
```

### `PermissionMode`

Class · `agnoclaw.runtime.permissions.PermissionMode`

Supported runtime permission modes.

```python
from agnoclaw import PermissionMode

PermissionMode(*values)
```

## `agnoclaw.runtime.policy`

### `AllowAllPolicyEngine`

Class · `agnoclaw.runtime.policy.AllowAllPolicyEngine`

Default policy behavior for local/standalone mode.

```python
from agnoclaw import AllowAllPolicyEngine

AllowAllPolicyEngine()
```

### `PolicyAction`

Class · `agnoclaw.runtime.policy.PolicyAction`

Enum where members are also (and must be) strings

```python
from agnoclaw import PolicyAction

PolicyAction(*values)
```

### `PolicyDecision`

Class · `agnoclaw.runtime.policy.PolicyDecision`

PolicyDecision(action: 'PolicyAction', reason_code: 'str', message: 'str' = '', constraints: 'dict[str, Any]' = <factory>, redactions: 'tuple[RedactionRule, ...]' = ())

```python
from agnoclaw import PolicyDecision

PolicyDecision(
    action: 'PolicyAction',
    reason_code: 'str',
    message: 'str' = '',
    constraints: 'dict[str, Any]' = <factory>,
    redactions: 'tuple[RedactionRule, ...]' = (),
) -> None
```

## `agnoclaw.runtime.postgres_store`

### `PostgresRuntimeStore`

Class · `agnoclaw.runtime.postgres_store.PostgresRuntimeStore`

Service RuntimeStore using PostgreSQL row locks and a bounded sync pool.

```python
from agnoclaw import PostgresRuntimeStore

PostgresRuntimeStore(
    dsn: 'str',
    *,
    min_pool_size: 'int' = 1,
    max_pool_size: 'int' = 10,
    max_waiting: 'int' = 100,
    pool_timeout_seconds: 'float' = 5.0,
    connect_timeout_seconds: 'float' = 10.0,
    application_name: 'str' = 'agnoclaw-runtime',
    fault_injector: 'Callable[[str], None] | None' = None,
    writer_authority: 'PostgresWriterAuthorityProvider | None' = None,
    writer_authority_check_timeout_seconds: 'float' = 2.0,
    writer_authority_safety_margin_seconds: 'float' = 1.0,
    writer_authority_max_transaction_seconds: 'float' = 30.0,
    read_only: 'bool' = False,
) -> 'None'
```

## `agnoclaw.runtime.reconciliation`

### `OperationReconciliationBatch`

Class · `agnoclaw.runtime.reconciliation.OperationReconciliationBatch`

OperationReconciliationBatch(items: 'tuple[OperationReconciliationItem, ...]', next_cursor: 'str | None')

```python
from agnoclaw import OperationReconciliationBatch

OperationReconciliationBatch(items: 'tuple[OperationReconciliationItem, ...]', next_cursor: 'str | None') -> None
```

### `OperationReconciliationCursorError`

Class · `agnoclaw.runtime.reconciliation.OperationReconciliationCursorError`

Stable error shape for public runtime operations.

```python
from agnoclaw import OperationReconciliationCursorError

OperationReconciliationCursorError() -> 'None'
```

### `OperationReconciliationItem`

Class · `agnoclaw.runtime.reconciliation.OperationReconciliationItem`

OperationReconciliationItem(operation_id: 'str', run_id: 'str', status: 'OperationReconciliationStatus', observed_revision: 'int', resulting_state: 'OperationState | None' = None, resulting_revision: 'int | None' = None, run_state: 'RunState | None' = None, reconciliation_id: 'str | None' = None, error_code: 'str | None' = None)

```python
from agnoclaw import OperationReconciliationItem

OperationReconciliationItem(
    operation_id: 'str',
    run_id: 'str',
    status: 'OperationReconciliationStatus',
    observed_revision: 'int',
    resulting_state: 'OperationState | None' = None,
    resulting_revision: 'int | None' = None,
    run_state: 'RunState | None' = None,
    reconciliation_id: 'str | None' = None,
    error_code: 'str | None' = None,
) -> None
```

### `OperationReconciliationObservation`

Class · `agnoclaw.runtime.reconciliation.OperationReconciliationObservation`

OperationReconciliationObservation(operation_id: 'str', expected_revision: 'int', operation_digest: 'str', verdict: 'OperationReconciliationVerdict', evidence_artifacts: 'tuple[ArtifactReference, ...]', result_reference: 'str | None' = None, provider_request_id: 'str | None' = None, observed_at: 'str' = <factory>)

```python
from agnoclaw import OperationReconciliationObservation

OperationReconciliationObservation(
    operation_id: 'str',
    expected_revision: 'int',
    operation_digest: 'str',
    verdict: 'OperationReconciliationVerdict',
    evidence_artifacts: 'tuple[ArtifactReference, ...]',
    result_reference: 'str | None' = None,
    provider_request_id: 'str | None' = None,
    observed_at: 'str' = <factory>,
) -> None
```

### `OperationReconciliationObserver`

Class · `agnoclaw.runtime.reconciliation.OperationReconciliationObserver`

Host-supplied, versioned observer that reads external state without dispatch.

```python
from agnoclaw import OperationReconciliationObserver

OperationReconciliationObserver(*args, **kwargs)
```

### `OperationReconciliationRequest`

Class · `agnoclaw.runtime.reconciliation.OperationReconciliationRequest`

OperationReconciliationRequest(record: 'OperationRecord')

```python
from agnoclaw import OperationReconciliationRequest

OperationReconciliationRequest(record: 'OperationRecord') -> None
```

### `OperationReconciliationStatus`

Class · `agnoclaw.runtime.reconciliation.OperationReconciliationStatus`

Enum where members are also (and must be) strings

```python
from agnoclaw import OperationReconciliationStatus

OperationReconciliationStatus(*values)
```

### `reconcile_pending_operations`

Function · `agnoclaw.runtime.reconciliation.reconcile_pending_operations`

Observe and CAS-settle one page; never infer an outcome or replay an effect.

```python
from agnoclaw import reconcile_pending_operations

reconcile_pending_operations(
    *,
    store: 'RuntimeStore',
    artifact_store: 'ArtifactStore',
    observer: 'OperationReconciliationObserver',
    observer_digest: 'str',
    recover_run: 'RecoverRun',
    default_owner: 'RunOwner',
    context: 'ExecutionContext | None' = None,
    cursor: 'str | None' = None,
    limit: 'int' = 25,
    concurrency: 'int' = 4,
    minimum_age_seconds: 'int' = 30,
) -> 'OperationReconciliationBatch'
```

## `agnoclaw.runtime.recovery`

### `recover_pending_runs`

Function · `agnoclaw.runtime.recovery.recover_pending_runs`

Claim and classify one bounded page of executable runs.

```python
from agnoclaw import recover_pending_runs

recover_pending_runs(
    *,
    store: 'RuntimeStore',
    recover_run: 'RecoverRun',
    default_owner: 'RunOwner',
    context: 'ExecutionContext | None' = None,
    cursor: 'str | None' = None,
    limit: 'int' = 25,
    concurrency: 'int' = 4,
    minimum_age_seconds: 'int' = 30,
) -> 'RuntimeRecoveryBatch'
```

### `RuntimeRecoveryBatch`

Class · `agnoclaw.runtime.recovery.RuntimeRecoveryBatch`

Ordered outcomes and an owner-bound cursor for the next page.

```python
from agnoclaw import RuntimeRecoveryBatch

RuntimeRecoveryBatch(items: 'tuple[RuntimeRecoveryItem, ...]', next_cursor: 'str | None') -> None
```

### `RuntimeRecoveryCursorError`

Class · `agnoclaw.runtime.recovery.RuntimeRecoveryCursorError`

Raised when a recovery cursor is malformed or belongs to another owner.

```python
from agnoclaw import RuntimeRecoveryCursorError

RuntimeRecoveryCursorError() -> 'None'
```

### `RuntimeRecoveryItem`

Class · `agnoclaw.runtime.recovery.RuntimeRecoveryItem`

One content-minimized recovery outcome.

```python
from agnoclaw import RuntimeRecoveryItem

RuntimeRecoveryItem(
    run_id: 'str',
    status: 'RuntimeRecoveryStatus',
    state: 'RunState',
    error_code: 'str | None' = None,
) -> None
```

### `RuntimeRecoveryStatus`

Class · `agnoclaw.runtime.recovery.RuntimeRecoveryStatus`

Safe per-run outcome from one bounded startup scan.

```python
from agnoclaw import RuntimeRecoveryStatus

RuntimeRecoveryStatus(*values)
```

## `agnoclaw.runtime.run_handle`

### `HarnessRun`

Class · `agnoclaw.runtime.run_handle.HarnessRun`

Stable run facade for observation, control, and declared child dispatch.

```python
from agnoclaw import HarnessRun

HarnessRun(
    *,
    result: 'Any' = None,
    stream: 'AsyncIterator[Any] | Iterator[Any] | None' = None,
    run_id: 'str | None' = None,
    store: 'RuntimeStore | None' = None,
    artifact_store: 'ArtifactStore | None' = None,
    owner: 'RunOwner | None' = None,
    waiter: 'WaitCallback | None' = None,
    canceller: 'CancelCallback | None' = None,
    commander: 'CommandCallback | None' = None,
    session_id: 'str | None' = None,
) -> 'None'
```

### `RunReconciliationRequiredError`

Class · `agnoclaw.runtime.run_handle.RunReconciliationRequiredError`

A wait stopped at an intentional ambiguous-effect reconciliation boundary.

```python
from agnoclaw import RunReconciliationRequiredError

RunReconciliationRequiredError(snapshot: 'RunSnapshot') -> 'None'
```

## `agnoclaw.runtime.scheduler`

### `InMemorySchedulerBackend`

Class · `agnoclaw.runtime.scheduler.InMemorySchedulerBackend`

In-process scheduler backend useful for tests and embedded demos.

```python
from agnoclaw import InMemorySchedulerBackend

InMemorySchedulerBackend() -> 'None'
```

### `JsonSchedulerBackend`

Class · `agnoclaw.runtime.scheduler.JsonSchedulerBackend`

Single-process JSON compatibility backend for jobs and run history.

```python
from agnoclaw import JsonSchedulerBackend

JsonSchedulerBackend(path: 'str | Path') -> 'None'
```

### `RuntimeSchedulerBackend`

Class · `agnoclaw.runtime.scheduler.RuntimeSchedulerBackend`

Durable scheduler adapter over the canonical RuntimeStore extension.

```python
from agnoclaw import RuntimeSchedulerBackend

RuntimeSchedulerBackend(store: 'DurableSchedulerStore') -> 'None'
```

### `scheduler_fence_path`

Function · `agnoclaw.runtime.scheduler.scheduler_fence_path`

Return the migration writer-fence marker for a JSON scheduler store.

```python
from agnoclaw import scheduler_fence_path

scheduler_fence_path(path: 'str | Path') -> 'Path'
```

### `scheduler_store_path`

Function · `agnoclaw.runtime.scheduler.scheduler_store_path`

Return the default local scheduler store path.

```python
from agnoclaw import scheduler_store_path

scheduler_store_path(root: 'str | Path | None' = None) -> 'Path'
```

### `SchedulerBackend`

Class · `agnoclaw.runtime.scheduler.SchedulerBackend`

Compatibility scheduler surface without leased worker ownership.

```python
from agnoclaw import SchedulerBackend

SchedulerBackend(*args, **kwargs)
```

### `SchedulerConfigurationError`

Class · `agnoclaw.runtime.scheduler.SchedulerConfigurationError`

A schedule cannot be evaluated safely.

```python
from agnoclaw import SchedulerConfigurationError

SchedulerConfigurationError(message: 'str', *, schedule: 'str', timezone: 'str')
```

### `SchedulerJob`

Class · `agnoclaw.runtime.scheduler.SchedulerJob`

A persisted scheduled harness job.

```python
from agnoclaw import SchedulerJob

SchedulerJob(
    name: 'str',
    schedule: 'str',
    prompt: 'str',
    skill: 'str | None' = None,
    isolated: 'bool' = False,
    model_id: 'str | None' = None,
    provider: 'str | None' = None,
    enabled: 'bool' = True,
    timezone: 'str' = 'UTC',
    max_retries: 'int' = 0,
    retry_delay_seconds: 'int' = 30,
    retry_backoff_multiplier: 'float' = 2.0,
    retry_max_delay_seconds: 'int' = 3600,
    retry_jitter_seconds: 'int' = 0,
    jitter_seconds: 'int' = 0,
    misfire_policy: 'str' = 'fire_once',
    misfire_grace_seconds: 'int' = 300,
    concurrency_key: 'str | None' = None,
    overlap_policy: 'str' = 'queue',
    metadata: 'dict[str, Any]' = <factory>,
    revision: 'int' = 0,
    next_run_at: 'str | None' = None,
    created_at: 'str' = <factory>,
    updated_at: 'str' = <factory>,
) -> None
```

### `SchedulerLeaseLostError`

Class · `agnoclaw.runtime.scheduler.SchedulerLeaseLostError`

The scheduler claim is stale, expired, or owned by another worker.

```python
from agnoclaw import SchedulerLeaseLostError

SchedulerLeaseLostError(*, run_id: 'str')
```

### `SchedulerMisfirePolicy`

Class · `agnoclaw.runtime.scheduler.SchedulerMisfirePolicy`

What to do when an occurrence is discovered after its nominal time.

```python
from agnoclaw import SchedulerMisfirePolicy

SchedulerMisfirePolicy(*values)
```

### `SchedulerOverlapPolicy`

Class · `agnoclaw.runtime.scheduler.SchedulerOverlapPolicy`

What to do when a job's concurrency group is already occupied.

```python
from agnoclaw import SchedulerOverlapPolicy

SchedulerOverlapPolicy(*values)
```

### `SchedulerRunClaim`

Class · `agnoclaw.runtime.scheduler.SchedulerRunClaim`

Fenced ownership of one exact scheduler attempt.

```python
from agnoclaw import SchedulerRunClaim

SchedulerRunClaim(
    record: 'SchedulerRunRecord',
    job: 'SchedulerJob',
    worker_id: 'str',
    claim_id: 'str',
    lease_token: 'str',
    fence_token: 'int',
    acquired_at: 'str',
    expires_at: 'str',
) -> None
```

### `SchedulerRunRecord`

Class · `agnoclaw.runtime.scheduler.SchedulerRunRecord`

A persisted scheduled job execution record.

```python
from agnoclaw import SchedulerRunRecord

SchedulerRunRecord(
    run_id: 'str',
    job_name: 'str',
    status: 'str',
    started_at: 'str',
    finished_at: 'str | None' = None,
    output: 'str | None' = None,
    error: 'str | None' = None,
    metadata: 'dict[str, Any]' = <factory>,
    occurrence_id: 'str | None' = None,
    attempt: 'int' = 1,
    scheduled_at: 'str | None' = None,
    available_at: 'str | None' = None,
    runtime_run_id: 'str | None' = None,
) -> None
```

## `agnoclaw.runtime.spec`

### `HarnessRuntimeManifest`

Class · `agnoclaw.runtime.spec.HarnessRuntimeManifest`

Content-minimized public evidence for one compiled harness configuration.

```python
from agnoclaw import HarnessRuntimeManifest

HarnessRuntimeManifest(
    schema_version: 'str',
    harness_name: 'str',
    agent_id: 'str | None',
    profile: 'str',
    spec_digest: 'str',
    resources: 'tuple[MaterializerDescriptor, ...]',
) -> None
```

## `agnoclaw.runtime.store`

### `DeadLetterAuditAction`

Class · `agnoclaw.runtime.store.DeadLetterAuditAction`

Enum where members are also (and must be) strings

```python
from agnoclaw import DeadLetterAuditAction

DeadLetterAuditAction(*values)
```

### `DeadLetterAuditRecord`

Class · `agnoclaw.runtime.store.DeadLetterAuditRecord`

DeadLetterAuditRecord(audit_sequence: 'int', audit_id: 'str', action: 'DeadLetterAuditAction', owner: 'RunOwner', operator_digest: 'str', authority_digest: 'str', reason_code: 'str', requested_after_outbox_id: 'int | None', requested_limit: 'int | None', result_count: 'int', first_outbox_id: 'int | None', last_outbox_id: 'int | None', outbox_id: 'int | None', run_id: 'str | None', expected_dead_lettered_at: 'str | None', delay_seconds: 'float', mutation_id: 'str | None', mutation_digest: 'str | None', created_at: 'str')

```python
from agnoclaw import DeadLetterAuditRecord

DeadLetterAuditRecord(
    audit_sequence: 'int',
    audit_id: 'str',
    action: 'DeadLetterAuditAction',
    owner: 'RunOwner',
    operator_digest: 'str',
    authority_digest: 'str',
    reason_code: 'str',
    requested_after_outbox_id: 'int | None',
    requested_limit: 'int | None',
    result_count: 'int',
    first_outbox_id: 'int | None',
    last_outbox_id: 'int | None',
    outbox_id: 'int | None',
    run_id: 'str | None',
    expected_dead_lettered_at: 'str | None',
    delay_seconds: 'float',
    mutation_id: 'str | None',
    mutation_digest: 'str | None',
    created_at: 'str',
) -> None
```

### `DeadLetterInspectionDecision`

Class · `agnoclaw.runtime.store.DeadLetterInspectionDecision`

DeadLetterInspectionDecision(items: 'tuple[DeadLetterItem, ...]', audit: 'DeadLetterAuditRecord')

```python
from agnoclaw import DeadLetterInspectionDecision

DeadLetterInspectionDecision(items: 'tuple[DeadLetterItem, ...]', audit: 'DeadLetterAuditRecord') -> None
```

### `DeadLetterItem`

Class · `agnoclaw.runtime.store.DeadLetterItem`

DeadLetterItem(outbox_id: 'int', run_id: 'str', sequence: 'int', event: 'RuntimeEvent', attempts: 'int', reason_code: 'str', dead_lettered_at: 'str')

```python
from agnoclaw import DeadLetterItem

DeadLetterItem(
    outbox_id: 'int',
    run_id: 'str',
    sequence: 'int',
    event: 'RuntimeEvent',
    attempts: 'int',
    reason_code: 'str',
    dead_lettered_at: 'str',
) -> None
```

### `DeadLetterRequeueDecision`

Class · `agnoclaw.runtime.store.DeadLetterRequeueDecision`

DeadLetterRequeueDecision(audit: 'DeadLetterAuditRecord', idempotent: 'bool' = False)

```python
from agnoclaw import DeadLetterRequeueDecision

DeadLetterRequeueDecision(audit: 'DeadLetterAuditRecord', idempotent: 'bool' = False) -> None
```

### `OutboxDeadLetterConflictError`

Class · `agnoclaw.runtime.store.OutboxDeadLetterConflictError`

Stable error shape for public runtime operations.

```python
from agnoclaw import OutboxDeadLetterConflictError

OutboxDeadLetterConflictError(outbox_id: 'int')
```

### `OutboxDeadLetterMutationConflictError`

Class · `agnoclaw.runtime.store.OutboxDeadLetterMutationConflictError`

Stable error shape for public runtime operations.

```python
from agnoclaw import OutboxDeadLetterMutationConflictError

OutboxDeadLetterMutationConflictError(mutation_id: 'str')
```

### `RunOwner`

Class · `agnoclaw.runtime.store.RunOwner`

Exact owner tuple used for store-level defense in depth.

```python
from agnoclaw import RunOwner

RunOwner(tenant_id: 'str | None', user_id: 'str | None') -> None
```

### `RuntimeStore`

Class · `agnoclaw.runtime.store.RuntimeStore`

One authoritative run/event/outbox extension boundary.

```python
from agnoclaw import RuntimeStore

RuntimeStore(*args, **kwargs)
```

### `SQLiteRuntimeStore`

Class · `agnoclaw.runtime.store.SQLiteRuntimeStore`

Transactional single-node RuntimeStore and schema migration authority.

```python
from agnoclaw import SQLiteRuntimeStore

SQLiteRuntimeStore(
    path: 'str | Path',
    *,
    busy_timeout_ms: 'int' = 5000,
    fault_injector: 'Callable[[str], None] | None' = None,
    read_only: 'bool' = False,
) -> 'None'
```

## `agnoclaw.runtime.telemetry`

### `OpenTelemetryRuntimeSink`

Class · `agnoclaw.runtime.telemetry.OpenTelemetryRuntimeSink`

Bridge safe batches to an OTel-enabled logger and optional Meter.

```python
from agnoclaw import OpenTelemetryRuntimeSink

OpenTelemetryRuntimeSink(*, logger: 'logging.Logger', meter: '_Meter | None' = None) -> 'None'
```

### `RuntimeTelemetryBatchExporter`

Class · `agnoclaw.runtime.telemetry.RuntimeTelemetryBatchExporter`

Runtime outbox exporter that strips arbitrary event bodies before delegation.

```python
from agnoclaw import RuntimeTelemetryBatchExporter

RuntimeTelemetryBatchExporter(*, sink: 'RuntimeTelemetrySink', policy: 'RuntimeTelemetryPolicy') -> 'None'
```

### `RuntimeTelemetryPolicy`

Class · `agnoclaw.runtime.telemetry.RuntimeTelemetryPolicy`

Projection policy; the identifier key never enters records or repr output.

```python
from agnoclaw import RuntimeTelemetryPolicy

RuntimeTelemetryPolicy(
    identifier_key: 'bytes',
    key_id: 'str' = 'default',
    service_name: 'str' = 'agnoclaw',
    include_event_linkage: 'bool' = True,
) -> None
```

## `agnoclaw.runtime.usage`

### `ChildBudgetAssessment`

Class · `agnoclaw.runtime.usage.ChildBudgetAssessment`

Post-dispatch comparison of reported provider usage to one child grant.

```python
from agnoclaw import ChildBudgetAssessment

ChildBudgetAssessment(
    measured: 'Any',
    exceeded_dimensions: 'tuple[str, ...]',
    unverified_dimensions: 'tuple[str, ...]',
) -> None
```

## `agnoclaw.skills.backends`

### `AutoApproveSkillInstallApprover`

Class · `agnoclaw.skills.backends.AutoApproveSkillInstallApprover`

Install approver that always returns True.

```python
from agnoclaw import AutoApproveSkillInstallApprover

AutoApproveSkillInstallApprover()
```

### `InteractiveSkillInstallApprover`

Class · `agnoclaw.skills.backends.InteractiveSkillInstallApprover`

Interactive terminal approver for skill installs.

```python
from agnoclaw import InteractiveSkillInstallApprover

InteractiveSkillInstallApprover()
```

## `agnoclaw.tools.backends`

### `CommandExecutor`

Class · `agnoclaw.tools.backends.CommandExecutor`

Backend interface for shell execution.

```python
from agnoclaw import CommandExecutor

CommandExecutor(*args, **kwargs)
```

### `LocalCommandExecutor`

Class · `agnoclaw.tools.backends.LocalCommandExecutor`

Host-local subprocess implementation for command execution.

```python
from agnoclaw import LocalCommandExecutor

LocalCommandExecutor(*, workspace_dir: 'str | Path | None' = None, max_background_tasks: 'int' = 16) -> 'None'
```

### `LocalWorkspaceAdapter`

Class · `agnoclaw.tools.backends.LocalWorkspaceAdapter`

Host-local pathlib implementation for file operations.

```python
from agnoclaw import LocalWorkspaceAdapter

LocalWorkspaceAdapter(workspace_dir: 'str | Path | None' = None) -> 'None'
```

### `normalize_sandbox_mode`

Function · `agnoclaw.tools.backends.normalize_sandbox_mode`

Normalize public sandbox mode aliases into a stable enum.

```python
from agnoclaw import normalize_sandbox_mode

normalize_sandbox_mode(mode: 'str | SandboxMode | None') -> 'SandboxMode'
```

### `SandboxMode`

Class · `agnoclaw.tools.backends.SandboxMode`

Session sandbox filesystem modes for built-in files/bash tools.

```python
from agnoclaw import SandboxMode

SandboxMode(*values)
```

### `WorkspaceAdapter`

Class · `agnoclaw.tools.backends.WorkspaceAdapter`

Backend interface for workspace/file operations.

```python
from agnoclaw import WorkspaceAdapter

WorkspaceAdapter(*args, **kwargs)
```

## `agnoclaw.tools.browser_backends`

### `BrowserBackend`

Class · `agnoclaw.tools.browser_backends.BrowserBackend`

Backend interface for browser/computer-use tool operations.

```python
from agnoclaw import BrowserBackend

BrowserBackend(*args, **kwargs)
```

### `LocalPlaywrightBrowserBackend`

Class · `agnoclaw.tools.browser_backends.LocalPlaywrightBrowserBackend`

Host-local Playwright backend preserving current browser toolkit behavior.

```python
from agnoclaw import LocalPlaywrightBrowserBackend

LocalPlaywrightBrowserBackend(
    *,
    headless: 'bool' = True,
    viewport_width: 'int' = 1280,
    viewport_height: 'int' = 720,
    network_policy: 'NetworkURLPolicy | None' = None,
) -> 'None'
```

## `agnoclaw.tools.tasks`

### `PlanSignalToolkit`

Class · `agnoclaw.tools.tasks.PlanSignalToolkit`

Structured plan-mode signals for questions and plan completion.

```python
from agnoclaw import PlanSignalToolkit

PlanSignalToolkit()
```

### `SubagentDefinition`

Class · `agnoclaw.tools.tasks.SubagentDefinition`

Pre-registered named subagent definition.

```python
from agnoclaw import SubagentDefinition

SubagentDefinition(
    description: 'str',
    prompt: 'str' = '',
    tools: 'list[str]' = <factory>,
    model: 'str | None' = None,
) -> None
```

## `agnoclaw.workspace`

### `Workspace`

Class · `agnoclaw.workspace.Workspace`

Represents an agent workspace directory with hierarchical parent chain.

```python
from agnoclaw import Workspace

Workspace(
    path: 'str | Path | None' = None,
    global_dir: 'str | Path | None' = None,
    project_dir: 'str | Path | None' = None,
)
```

## `collections.abc`

### `CapabilityInvoker`

Type Alias · `collections.abc.Callable`

Versioned public value.

```python
from agnoclaw import CapabilityInvoker

CapabilityInvoker = collections.abc.Callable[[typing.Any, collections.abc.Mapping[str, typing.Any]], typing.Any | collections.abc.Awaitable[typing.Any]]
```

### `EvaluationInputBuilder`

Type Alias · `collections.abc.Callable`

Versioned public value.

```python
from agnoclaw import EvaluationInputBuilder

EvaluationInputBuilder = collections.abc.Callable[[agnoclaw.improvement_corpus.EvaluationCase], typing.Any]
```

### `EvaluationOutputBuilder`

Type Alias · `collections.abc.Callable`

Versioned public value.

```python
from agnoclaw import EvaluationOutputBuilder

EvaluationOutputBuilder = collections.abc.Callable[[typing.Any], typing.Any]
```

### `ProcessEvaluationHandler`

Type Alias · `collections.abc.Callable`

Versioned public value.

```python
from agnoclaw import ProcessEvaluationHandler

ProcessEvaluationHandler = collections.abc.Callable[[agnoclaw.improvement_corpus.EvaluationCase], agnoclaw.improvement_runner.EvaluationRollout | collections.abc.Awaitable[agnoclaw.improvement_runner.EvaluationRollout]]
```

## `types`

### `RunCommand`

Type Alias · `types.RunCommand`

Versioned public value.

```python
from agnoclaw import RunCommand

RunCommand = agnoclaw.commands.Pause | agnoclaw.commands.Resume | agnoclaw.commands.Respond | agnoclaw.commands.Steer | agnoclaw.commands.Fork
```
